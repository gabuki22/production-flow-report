# -*- coding: utf-8 -*-
"""앱 설정.

**도메인을 바꿀 때 가장 먼저 고치는 곳이 이 파일이다.**

다만 이 파일만 고치면 끝나지 않는다. 컬럼명은 metrics.py 안에도 박혀 있어서,
config를 다 바꾸고 앱을 켜면 거기서 깨진다. **깨지는 것이 정상이다.**
순서는 DESIGN.md §4-6에 있다.

    1. 여기 FUNNEL_STEPS 를 내 단계로 바꾼다
    2. load.py 가 내 파일을 읽게 한다
    3. 앱을 켠다 -> 깨진다
    4. metrics.py 의 컬럼명을 하나씩 맞춘다
    5. validate.py 에서 내 데이터에 맞지 않는 검증을 뺀다

────────────────────────────────────────────────────────────────────
7주차 토요일에 만든 「명세 5행」이 여기로 들어온다.

  명세 1행  데이터                 ->  DATASET · PERIOD · TABLES
  명세 2행  퍼널 단계              ->  FUNNEL_STEPS · FUNNEL_LABELS
  명세 3행  주지표 + 가드레일       ->  metrics.py 에서 계산
  명세 4행  임계값 · 최소 표본      ->  THRESHOLDS · MIN_SAMPLE
  명세 5행  유지 / 이탈의 정의      ->  RETENTION_STEPS
────────────────────────────────────────────────────────────────────

지금 값은 **통신사 예시**다. ★ 표시가 붙은 것을 내 것으로 바꾼다.
자기 데이터가 없으면 그대로 두고 진행해도 된다.
"""
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
DATA_DIR = ROOT / "data"          # 데이터 파일
RUNS_DIR = ROOT / "runs"          # 실행 이력
# 사람이 쓴 장을 담는 곳 — 메모리에만 두면 새로고침에 사라진다 (2026-09-05)
DRAFTS_DIR = ROOT / "drafts"
ASSETS_DIR = ROOT / "assets"      # 폰트 등

# ── 데이터 ────────────────────────────────────────────────────────
# ★ Day1 — 명세 1행  (출처: mydomain/ 명세 5행 · 프로젝트3위키/나의_성장퍼널.md)
DATASET = "성우산업 생산 흐름"
PERIOD = ("2025-09-01", "2026-08-29")
# ★ 2026-09-05 — **앱 이름을 우리 것으로 바꿨다.**
#   물음: *"제목은 왜 성장퍼널이야?"*
#   골격이 통신사 것이라 이름도 통신사 말이었다. 퍼널 단계도 지표도
#   이탈→긴급품도 전부 우리 것으로 갈아엎었으면서 **제목만 강사 것이었다.**
#   이름이 안 맞으면 열 때마다 남의 앱을 여는 기분이 든다.
#
#   ⚠️ 이름은 여기 한 곳에만 둔다. 화면·PDF·메일 제목이 각자 문자열을 들고
#      있으면 반드시 한쪽만 고치는 날이 온다.
APP_NAME = "생산 흐름 리포트"
APP_TAGLINE = "발주가 어디서 막히고 얼마나 밀리는가"
SUBTITLE = "발주부터 출하·미납까지 어디서 막히고 얼마나 새는가"
GRAIN = "발주 1건 (수주ID)"
# ★ 세는 단위. 골격은 "명"이 박혀 있었다(통신사는 사람을 센다).
#   우리는 사람이 아니라 **수주를 세므로** 건이다. 차트·문장이 이 값을 쓴다.
#   단위가 틀리면 값이 맞아도 읽는 사람이 다른 것을 본다.
UNIT = "건"
TODAY = "2026-08-29"          # 데이터 추출 기준일. 유효구간을 자를 때 쓴다.

# ★ 내 테이블 목록. 파일명(확장자 제외)과 같아야 한다.
#   9개일 이유가 없다 — 조인할 테이블이 없으면 조인 함정도 없다.
TABLES = [
    "orders",            # 수주 96,000건 — 퍼널의 분모
    "order_events",      # 공정 이벤트 572,664건 — 퍼널 6단계 + 미납확인
    "defect_events",     # 검사 이벤트 92,518건 — 불량률(가드레일)
    "product_cost_bom",  # 제품 원가 1,200행 — 마스터
    "worklog_검사",       # 검사 작업일보 135,017건 — 실제 처리량
    "experiments",              # 실험 3건 — ⚠️ 설계만 있고 실행 기록이 아니다
    "experiment_assignments",   # 배정 19,064건 — 그레인이 수주ID 라 퍼널과 조인된다
]

# ★ 분해 축 — funnel_by() 가 쪼갤 컬럼. **orders 에 있는 컬럼**이어야 한다.
#   쪼개는 기준은 "격차가 보이면 손을 쓸 수 있는가"다.
#     고객사  물량 배분·납기 협의를 우리가 건다        → 쓸 수 있다
#     차종    라인 편성이 차종 단위로 잡힌다            → 쓸 수 있다
#     색상    도장 묶음 순서가 색상으로 정해진다        → 쓸 수 있다
#   제품도번(1,200종)은 넣지 않았다 — 쪼개면 칸마다 표본이 MIN_SAMPLE 밑으로 간다.
#
#   ★ 2026-09-03 Day3 프롬프트 1 — 후보 3개를 실측(칸 수·최소 칸·격차)으로 내고
#     **고객사를 직접 골랐다.** 첫 번째가 판정을 붙이는 기본 축이다.
#     병목 구간(공정작업 → 출하)에서 격차: 고객사 0.57%p · 차종 0.28%p · 색상 0.14%p.
#     셋 다 1%p 미만 — 교안: *"격차가 거의 없다 → 그것도 결과다. 한계에 적는다."*
#     이 축으로는 안 갈린다는 것을 확인한 것이지, 축이 틀린 것이 아니다.
DIMS = ["고객사", "차종", "색상"]

# ── 퍼널 ──────────────────────────────────────────────────────────
# ★ Day1 — 명세 2행. 순서가 곧 단계 순서다.
#   단계 수는 도메인이 정한다. 6개일 이유가 없다 — 3개여도 8개여도 된다.
#   퍼널이 아니면 퍼널이라고 부르지 않는다: 앞 단계를 반드시 거치는가?
# ★ 2026-09-05 — `공정작업` 한 덩어리를 **실제 6공정**으로 쪼갰다.
#   왜: 통과율 퍼널이 전 구간 탈락 최대 1.6%로 평평한데, 같은 퍼널을 날짜로 재면
#   공정작업 → 출하 한 구간이 **전체 37일 중 25일(68%)** 이었다.
#   그 25일 안이 안 보여서 "병목이 어디냐"에 *"공정 안 어딘가"* 까지만 답이 됐다.
FUNNEL_STEPS = ["발주접수", "영업계획", "생산계획", "지시서작성",
                "사출", "지그삽입", "도장", "레이저", "인쇄", "검사", "출하"]
FUNNEL_LABELS = {
    "발주접수": "발주 접수",
    "영업계획": "영업 계획",
    "생산계획": "생산 계획",
    "지시서작성": "지시서 작성",
    "사출": "사출",
    "지그삽입": "지그 삽입",
    "도장": "도장",
    "레이저": "레이저",
    "인쇄": "인쇄",
    "검사": "검사",
    "출하": "출하",
}
# 공정에 착수한 건 = 첫 공정 도달분. 불량·작업일보가 이 기준을 쓴다.
FIRST_PROCESS = "사출"
PROCESS_STEPS = ["사출", "지그삽입", "도장", "레이저", "인쇄", "검사"]

# 퍼널 밖 — 순환의 입구. 단계가 아니라 되돌아가는 자리라 FUNNEL_STEPS 에 넣지 않는다.
# (아래 둘도 아직 골격이 참조하지 않는다 — Day2 유지 퍼널에서 배선한다)
STAGE_UNPAID = "미납확인"

# 완주 코호트 커트라인 — 납기가 수주+25~60일이라 그 전엔 완주 여부를 판정할 수 없다.
# (7주차 Day5 생존편향 — 관측 기간이 다른 대상을 비교하지 않는다)
COHORT_DAYS = 61

# ── 유지 퍼널 ─────────────────────────────────────────────────────
# ★ Day2 — 명세 5행. 7주차 토요일에 내가 정한 유지·이탈의 정의를 옮긴다.
#
#   획득 퍼널은 단계가 주어졌지만 유지는 순서가 없다. 내가 정의해야 한다.
#   각 단계는 (이름, 설명) 이고, 계산은 metrics.retention_funnel() 에서 한다.
#
#   예시 - 통신사:
#     [("가입", "전체 고객"),
#      ("활성", "마지막 달에 사용 기록이 있음"),
#      ("이탈", "is_churned = True")]
#
#   이탈 개념이 없는 도메인이면 재이용률·반복 요청률로 바꾼다. (DESIGN.md §4-4)
#
#   ★ 우리 정의(명세 5행 · 2026-09-02 개정) —
#      "납기일이 지났는데 아직 출하되지 않은 수주" = **긴급품**
#
#   ⚠️ **이것은 고객 유지가 아니라 수주 생존이다.** 통신사의 유지는 사람이 남는가지만,
#      우리 수주는 출하되면 거기서 끝난다. 그래서 "들어온 수주가 출하까지 살아남는가"로
#      풀었다. 이름을 유지 퍼널이라 쓰되 **무엇의 유지인지 화면에 밝힌다** —
#      퍼널이 아닌 것을 퍼널이라 부르지 않는 것과 같은 이유다.
#
#   각 단계는 앞 단계의 부분집합이다(중첩). 안 그러면 퍼널이 아니라 분류다.
#     납기도래   ⊃ 출하완료 ⊃ 납기내출하
#   긴급품은 단계가 아니라 **아직 안 나간 자리**다 — 빠져나간 게 아니라 남아 있다.
#   그래서 여기 넣지 않고 따로 센다.
#   ★ 2026-09-02 — 교안 프롬프트 5(후보 나열)를 제대로 돌려서 고른 결과다.
#     그 전에는 내가 정한 3단계였다(납기도래 → 출하완료 → 납기내출하).
#
#     선택: **미납 단계를 끼운다.**
#     `미납확인` 은 획득 퍼널에서는 순환의 입구라 뺐지만, 유지 축에서는
#     *"한 번에 통과했나"* 를 묻는 **우리 도메인에만 있는 신호**다.
#
#     ⚠️ 4단계로 넣어 봤더니 마지막 구간이 **100.0%** 였다 —
#        미납을 안 겪고 출하된 수주는 **전부 납기 내**여서 `출하완료` 와
#        `납기내출하` 가 같은 집합이 된다(뒤집으면: 납기를 넘긴 것은 전부
#        미납확인을 거쳤다). 아무것도 거르지 않는 단계라 뺐다.
#        교안 실습 A 확인 항목 — *"전환율이 0%나 100%인 단계가 없는가"*
RETENTION_STEPS: list[tuple[str, str]] = [
    ("납기도래", "완주 코호트 중 납기일이 이미 지난 수주 — 판정 가능한 모집단"),
    ("미납없음", "미납확인을 한 번도 거치지 않은 수주 — 한 번에 통과한 것"),
    ("납기내출하", "납기일 안에 출하된 수주 — 약속을 지킨 것"),
]
# ★ 2026-09-02 정정 — **우리 도메인에는 "이탈"이 없다.**
#
#   현장 확인: *"납기가 지나면 최대한 빨리 납품하려고 노력해야함. 긴급품이 됨"*
#
#   통신사의 이탈은 고객이 **떠나서 사라지는 것**이다. 우리 수주는 납기가 지나도
#   사라지지 않는다 — **더 급해질 뿐이다.** 없어지는 게 아니라 우선순위가 올라간다.
#   교안 〖내 도메인이라면〗: *"'유지'라는 말이 안 맞는 도메인도 많습니다."*
#
#   그래서 이름을 **긴급품**으로 바꾸고 유예도 없앴다(30일 → 0).
#   현장은 납기 다음 날부터 급하지, 한 달을 기다려 주지 않는다.
#   유예 30일 3,232건(4.34%) → 유예 0 **3,403건(4.57%)**
#
#   ⚠️ 늦게라도 출하된 3,217건은 **긴급품이 아니다** — 이미 나갔다.
#      그건 `지연출하` 로 따로 센다. 둘을 합치면 1 − 납기 준수율과 같은 값이 되어
#      지표 카드가 같은 것을 두 번 말하게 된다.
URGENT_GRACE_DAYS = 0
URGENT_DEF = "납기일이 지났는데 아직 출하되지 않은 수주 — 긴급품이 된다"

# ── 비즈니스 파라미터 ─────────────────────────────────────────────
# 전부 **가정값**이다. 실측 테이블에서 유도하지 않는다.
# 광고비(ad_spend)는 채널 효율(ROAS·CPC) 전용이고, 총액을 가입자 수로 나눠
# CAC를 만들지 않는다 — 광고비는 개인 단위로 추적되지 않아 가입과 이을 수 없다.
# 리포트에 이 값이 들어가는 문장에는 "가정값 기반"을 붙인다. -> DESIGN.md §4-3
#
# ★ 비용 개념이 없는 도메인이면 투입 공수(인시)로 바꿔도 된다. 금액이 아니어도
#   "무엇을 얼마나 썼는가"면 된다.
# 🔴 골격 원본에는 여기 통신사 가정값 셋(CHANNEL_CAC · CONTRIBUTION_MARGIN ·
#    LTV_MONTHS)이 있었다. **지웠다.** 우리는 획득 채널(검색·디스플레이…) 개념이
#    없어서 그 값들은 어떤 문장으로도 우리 것이 되지 않는다. 안 쓰는 값을 남겨 두면
#    Day3 선택 과제를 켜는 순간 조용히 쓰인다 — 남의 가정이 우리 리포트에 실린다.
#
# ★ 대신 우리 도메인의 같은 구조를 넣는다.
#   통신사   유효 CAC = 획득비용 ÷ 유지율     "싸게 데려와도 안 남으면 다시 채워야 한다"
#   우리     유효 원가 = 총원가 ÷ 양품률       "싸게 만들어도 불량 나면 다시 만들어야 한다"
#   구조가 같고, 순위가 뒤집히는 것도 같다. → metrics.channel_efficiency()
#
# 원가 배부 비율 — 현장 기준(2026-08-29). BOM 은 재료비까지만 도번별로 쌓이고,
# 인건·가공은 도번별로 안 잡혀 **매출 대비 총액 배부**한다.
#   ⚠️ product_cost_bom 의 인건비·가공비·영업이익·이익률 컬럼은 쓰지 않는다.
#      그 컬럼들은 도번당 몇 원 수준이라 배부 전 값이고, 그대로 쓰면 이익률이
#      60%대로 나온다(제조업에서 나올 수 없는 값이다 — 8/29에 한 번 걸렀다).
COST_RATIO = {"재료비": 0.40, "인건비": 0.30, "가공비": 0.225}
# 나머지 7.5% 가 영업이익이다. 배부 비율은 가정값이고, 리포트에 들어가면
# "가정값 기반"을 문장에 남긴다. → report/sections.py _s7_limits()

# ── 지표 임계값 ───────────────────────────────────────────────────
# ★ Day1 — 명세 4행. **비워 두었다. 내가 정한다.**
#
#   남이 정해줄 수 없는 유일한 값이다. 업계 벤치마크·직전 기간·목표치 중
#   무엇을 근거로 삼았는지 위키 지표 정의서에 한 줄로 적는다.
#   근거 없는 임계값은 남의 것이다.
#
#   지금은 비어 있어서 지표 카드가 전부 "정상"으로 뜬다. 채우면 색이 갈린다.
#
#   형식:  "지표이름": {"경고": 값, "위험": 값}
#   예시 - 통신사:
#     "전환율": {"경고": 3.0, "위험": 2.5},      # % 미만이면
#     "이탈률": {"경고": 18.0, "위험": 22.0},    # % 초과면
#   근거 — 현장 기준 (2026-08-29 대화에서 정한 값).
#   교안이 든 근거 4종 중 **"조직의 합의"**. 전문 → 프로젝트3위키/나의_성장퍼널.md
#   명세 4행 · mydomain/notes/03_지표정의.md
#
#     납기 준수율 90/80 — "납기는 고객과의 약속이라 90%는 지켜져야 하는 선이고,
#       80%를 밑돌면 거래 자체가 위태롭다." 통계적 변동폭(±1.96%p)이 아니라
#       **사업이 감내할 수 있는 선**에서 그었다.
#     불량률 4/5 — "보통 2~3%"라는 현장 감각. 평상시(2.50%)와
#       스파이크 포함(3.56%)을 정확히 가른다. **방향이 반대**라 부등호가 뒤집힌다
#       (metrics.status_of 의 higher_is_worse 에 "불량률"이 이미 들어 있다).
#
#   ※ 교안 프롬프트 4 는 "오늘은 비워 두고 목요일에 정한다"고 했지만, 우리는 8/29에
#     근거까지 정해 뒀으므로 채운다. 수업 진도를 그대로 따르려면 {} 로 되돌리면 된다.
#   ★ 2026-09-02 추가 — 지표 카드 4개 중 2개가 임계값 없이 **무조건 초록**이었다.
#     교안 경고 그대로: *"비어 있으면 전부 정상으로 뜬다."* 판정이 아니라 기본값이었다.
#
#     현장 지시: **긴급품 비율·지연 출하율 둘 다 10%.**
#     숫자를 하나만 주셨으므로 **경고와 위험을 같은 값**으로 둔다 —
#     "10%를 넘으면 바로 위험" 이라는 한 줄 규칙이다.
#     ⚠️ 이러면 **경고 구간이 없다.** 중간에 한 번 짚고 넘어가고 싶으시면
#        경고를 10보다 낮은 값으로 내리면 된다. 없는 숫자를 내가 만들지 않았다.
#
#     참고 실측(월 12개): 긴급품 평균 4.83% (4.32~6.96) · 지연 평균 3.67% (0.27~4.77)
#     → 10%는 최악의 달보다도 위다. 지금 데이터로는 어느 달도 안 걸린다.
#       큰 사고만 잡는 선이다.
THRESHOLDS: dict[str, dict[str, float]] = {
    "납기 준수율":   {"경고": 90.0, "위험": 80.0},
    "불량률":        {"경고": 4.0,  "위험": 5.0},
    "긴급품 비율":   {"경고": 10.0, "위험": 10.0},
    "지연 출하율":   {"경고": 10.0, "위험": 10.0},
}

# ★ 이보다 표본이 적으면 "표본 부족"으로 보고 지표를 계산하지 않는다.
#   내 상황에 맞게 낮춰도 된다. **다만 없애지는 않는다.** -> DESIGN.md §4-5
#
#   근거(2026-08-29): 월 분모 평균 6,582건. 100건이면 월 단위 판정이 되고
#   더 쪼개면 못 쓴다. 교안 기본값 1,000 을 그대로 쓰지 않은 이유 —
#   세그먼트(고객사·차종·색상)로 쪼개면 1,000을 넘는 칸이 거의 없어진다.
MIN_SAMPLE = 100

# ── 납기 기준 지표가 확정되는 데 걸리는 시간 ──────────────────────
# ★ 2026-09-04 — **긴급품 비율과 지연 출하율은 납기일에 확정되지 않는다.**
#   납기를 넘긴 수주는 나중에라도 출하되는데, 실측하면 **납기 뒤 최대 24일**
#   (중앙 13일)에 걸쳐 나간다. 그래서 데이터 끝에 가까운 달은
#
#       아직 안 나간 것  →  긴급품으로 세어  **과대**
#       늦게 나갈 것     →  지연 분모에서 빠져 **과소**
#
#   실제로 2026-08 은 긴급품 4.5%대 → 6.82%, 지연 4.2% → 2.51% 로
#   **둘이 반대로 튀었다.** 개선이 아니라 관찰 기간이 모자란 것이다.
#   납기 준수율은 이 문제가 없다 — 납기일이 지나는 순간 판정이 끝난다.
#
#   24일(실측 최대) + 1일 여유. 이만큼 안 지난 달은 두 지표를 계산하지 않는다.
# ── 게이트 판단 근거의 최소 길이 ──────────────────────────────────
# ★ 2026-09-04 (Day3 프롬프트 11) — 저장된 실행을 열어 보니 게이트 1·2 가 둘 다
#   note="" 였다. 통과 시각과 통과자는 남았는데 **무엇을 보고 통과시켰는지**가
#   없다. 그러면 기록이 아니라 도장이다. 게이트는 앱이 판단하지 않는 자리이고,
#   사람이 무엇을 봤는지가 유일한 산출물이다.
#   10자 — "이상 없음"(5자)은 막고 "직전 기간과 유사"(9자)는 통과할 만한 선.
# ── 지표 정의 (화면에서 st.popover 로 펼친다) ─────────────────────
# ★ 2026-09-04 (Day3 프롬프트 13) — 화면에는 숫자만 있고 **그 숫자가 무엇인지**가
#   없었다. 회의에서 "납기 준수율 91%"가 인용될 때 분모가 무엇인지 아는 사람과
#   모르는 사람이 같은 문장을 다르게 읽는다.
#
#   ⚠️ 정본은 notes/03_지표정의.md 다. 여기는 **화면에 띄울 요약**이고,
#      값이 갈리면 노트가 이긴다. 정의를 두 곳에서 고치게 되므로 늘리지 않는다.
#      (분모를 왜 이렇게 정했는지 같은 긴 이야기는 노트에만 둔다)
METRIC_DEFS = {
    "납기 준수율": {
        "식": "납기일 이내 출하 수주 ÷ 납기일이 도래한 수주",
        "그레인": "수주 1건 (수주ID) — 이벤트 행이 아니라 고유값",
        "기준일": "납기일",
        # ★ 2026-09-05 — 물음: *"납기 준수율과 퍼널 전환율은 뭘 뜻하는지 모르겠어"*
        #   대시보드에서 둘 중 하나를 고르라고 하면서 뜻을 안 적어 뒀다.
        #   **고르는 사람이 뜻을 모르면 그 선택지는 없는 것과 같다.**
        "주의": "**퍼널 전환율과 다른 것**이다 — 전환율은 *다음 단계로 넘어갔는가*"
                "(과정)이고, 준수율은 *약속한 날짜까지 나갔는가*(결과)다. "
                "늦게라도 나가면 전환율은 통과지만 준수율은 실패다. "
                "분모를 '출하한 건만'으로 바꾸면 94.9%로 오르는데, "
                "어려운 건을 미룰수록 지표가 올라가는 분모라 쓰지 않는다.",
    },
    "불량률": {
        "식": "불량수량 합 ÷ 검사수량 합 (수량 가중)",
        "그레인": "검사 1건",
        "기준일": "검사일",
        "주의": "건수 평균이 아니라 수량 가중이다. 큰 로트 한 건이 "
                "작은 로트 백 건보다 무겁다.",
    },
    "긴급품 비율": {
        "식": "납기가 지났는데 아직 미출하인 수주 ÷ 납기 도래 수주",
        "그레인": "수주 1건",
        "기준일": "납기일 (유예 0일)",
        "주의": "이탈이 아니다. 제조 수주는 사라지지 않고 급해진다. "
                "납기 뒤 최대 24일에 걸쳐 나가므로 최근 달은 과대로 찍힌다.",
    },
    "지연 출하율": {
        "식": "납기 이후에 출하된 수주 ÷ 출하된 수주",
        "그레인": "수주 1건",
        "기준일": "출하일",
        "주의": "긴급품과 더하면 1 − 납기 준수율이 된다. 둘을 같이 인용하면 "
                "같은 실패를 두 번 세는 것으로 읽힌다.",
    },
}

GATE_NOTE_MIN = 10

# ── 빠른 근거 (2026-09-05) ────────────────────────────────────────
#   근거 칸을 매번 손으로 채우는 것이 통과를 막는 벽이 됐다. 그렇다고 칸을
#   없애면 2026-09-04 에 고친 자리로 되돌아간다 — 그때 기록을 열어 보니
#   게이트 1·2 가 둘 다 note="" 였고, "통과 ✓" 만 있고 무엇을 보고 넘겼는지가
#   없었다. 나중에 이 실행을 꺼낸 사람은 "왜 통과였지"에 답할 수 없다.
#
#   ★ 그래서 **없애는 대신 한 번에 고르게** 한다. 한 번 누르면 통과 버튼이
#     바로 살아난다 — 자동으로 채워 넣는 것이 아니라 **사람이 고른 문장**이다.
#     코드가 몰래 적어 넣으면 그건 다시 통과 도장이다.
#
#   문구를 여기 두는 이유 — 화면 세 곳(게이트 1·2·3)에 흩어 놓으면 말이 갈린다.
#   현장에서 자주 쓰는 판단이 바뀌면 **이 표만** 고친다.
GATE_PRESETS = {
    1: {
        "warn": ["경고를 확인했고 해당 행만 빼고 진행합니다",
                 "경고를 확인했으나 판정에 영향이 없다고 봅니다",
                 "경고는 추출 시점 차이라 원본 문제가 아닙니다"],
        "clean": ["검증 4종 모두 통과 — 이상 없습니다",
                  "행 수·컬럼·날짜 범위 확인했고 이상 없습니다"],
    },
    2: ["직전 기간과 자릿수·추세 모두 유사합니다",
        "값이 상식 범위 안이라 통과시킵니다",
        "퍼널 단계가 단조 감소하고 역전이 없습니다"],
    3: ["초안 확정 · 실제 발송 없음 · 통과 기록만 남깁니다",
        "합성 데이터라 실제 보고에 쓸 수 없음을 한계에 적었습니다"],
}

DUE_SETTLE_DAYS = 25

# ★ 유효 구간 — 월별 추이에서 **분모가 얇은 달**을 뺀다 (Day3 프롬프트 10)
#
#   MIN_SAMPLE 만으로는 못 잡는다. 실측: 첫 달(2025-09) 분모가 **115건**인데
#   115 > 100 이라 그냥 통과한다. 그런데 다음 달은 4,394건 — **38배**다.
#   같은 91.1% 라도 무게가 다르다.
#
#   왜 첫 달만 얇은가 — monthly() 는 **납기월** 기준인데 납기가 수주+25~60일이라
#   "9월에 수주해서 9월에 납기가 온 것"만 잡힌다. **데이터 시작 경계**다.
#   교안 부록 C 가 말한 우측 절단의 **왼쪽 판**이다 — 최근 것만 문제가 아니다.
#
#   ⚠️ 절대 건수가 아니라 **중앙값 대비 비율**로 자른다. 절대값으로 박으면
#      데이터가 늘었을 때 손으로 다시 고쳐야 하고, 결국 아무도 안 고친다.
#   현장 기준 2026-09-03: *"분모가 얇은 달을 자른다."*
THIN_MONTH_RATIO = 0.7   # 월 분모가 중앙값의 70% 미만이면 추이에서 뺀다

# ★ 실험을 믿기 위한 최소 기간. 표본만 차면 되는 것이 아니다 —
#   짧게 돌리면 **반짝 효과**가 그대로 남는다. 새 절차를 넣으면 처음엔 다들
#   신경 써서 잘 지키다가, 익숙해지면 원래대로 돌아간다. 그 기간에만 재면
#   절차가 좋아서 오른 것인지 사람이 긴장해서 오른 것인지 가릴 수 없다.
#
#   ★ 2026-09-03 정정 — 전에는 **41일**이었다. 표본 수에서 역산한 값이다
#     (그룹당 5,298건 ÷ 일평균 260건). 계산은 맞지만 **현장에서 온 값이 아니었다.**
#
#   현장 기준: *"새 절차를 넣으면 2주면 긴장이 풀린다."*
#   → 긴장이 풀린 뒤로도 2주를 더 봐야 평상시 모습이 나온다. **4주 = 28일.**
#
#   ⚠️ 표본 조건(MIN_SAMPLE)과 **따로 건다.** 기간이 차도 표본이 모자랄 수 있고
#      그 반대도 가능하다. 둘 중 하나만 걸려도 못 믿는 것은 같다.
MIN_EXP_DAYS = 28

# ── 판정 기준 (Day3 실습 C) ───────────────────────────────────────
# ★ 주지표가 이만큼은 움직여야 "효과가 있다"고 본다.
#
#   교안: *"움직임 기준은 평소 변동 폭을 실제로 보고 정한다."*
#   실측 — 스파이크 미발생률은 **아무것도 안 해도** 월마다 이만큼 흔들린다:
#       전월 대비 |Δ| 중앙 1.51%p · 최대 5.19%p · 12개월 범위 93.17~98.36%
#
#   현장 기준 2026-09-03: **1.5%p — 흔들림만큼만 넘으면 인정한다.**
#   낮게 잡으면 없는 효과를 있다고 하고, 높게 잡으면 있는 효과를 없다고 한다.
#   평소 흔들림과 같은 크기에 그은 것이라, **그보다 작은 차이는 우연과 구분되지 않는다.**
EFFECT_MIN_PP = 1.5

# ★ 가드레일이 이만큼 넘게 나빠지면 주지표가 좋아도 성공이 아니다.
#   2026-09-03 현장 기준: **3%p — 느슨하게.** 어지간한 하락은 감수한다
#   (불량을 잡는 것이 더 급할 때 쓰는 선).
#   ⚠️ 전에는 2%p 였다 — 7주차 가설 H1 문구에서 가져온 값이다.
#      납기 준수율의 평소 |Δ| 중앙값이 0.40%p 이므로 3%p 는 그 7.5배 —
#      **진짜 악화만 잡는다.**
GUARDRAIL_TOLERANCE = 0.03

# ★ 유효 원가를 무엇으로 쪼갤지 → metrics.channel_efficiency()
#   색상으로 잡은 이유: 도장 불량률이 색상마다 다르고, 색상 묶음 순서는
#   우리가 바꿀 수 있는 것이다. 바꿀 수 없는 축으로 쪼개면 볼 이유가 적다.
EFFICIENCY_DIM = "색상"

# ── 납기 전망 (5번째 화면) ────────────────────────────────────────
# ★ 골격에 없는 **우리 도메인 고유 화면**이다. 강사 앱에 `5_예산전망` 이 있는 것과
#   같은 자리 — 교안: *"골격은 출발점이지 종착점이 아니다"*.
#
# 왜 납기인가 — 우리 퍼널은 단계별 탈락이 최대 1.6%로 평평해서 **통과율로는
#   문제가 안 보인다.** 같은 데이터를 납기 축에서 보면 지연 4.0% · 이탈 4.3%가
#   드러난다. 그 축을 화면 하나로 앞세운다.
#
# ⚠️ **예측이 아니라 산술 전망이다.** 지금 밀린 것과 앞으로 올 납기를,
#   최근 실측 처리 속도로 나눈 것뿐이다. 계절성·설비 정지·긴급 삽입을 모른다.
OUTLOOK_WEEKS = 8            # 몇 주 앞까지 볼 것인가
THROUGHPUT_WINDOW_DAYS = 28  # 처리 속도를 최근 며칠로 잴 것인가
#   4주로 잡은 이유: 짧게 잡으면 한 주의 사정(연휴·설비 정지)이 그대로 속도가 되고,
#   길게 잡으면 지금 현장 상태를 못 따라간다. 바꿀 수 있는 값이라 여기 둔다.

# 미출하 수주를 납기까지 남은 일수로 나누는 구간. (하한, 상한, 이름, 상태)
#   상태는 config.COLORS 의 키다 — 판정에만 쓰고 장식에는 쓰지 않는다.
#   ★ 30일은 이제 '이탈 경계'가 아니라 **오래 밀린 긴급품**을 가르는 선이다.
LONG_URGENT_DAYS = 30
BACKLOG_BINS = [
    (None, -(LONG_URGENT_DAYS + 1), f"긴급 · {LONG_URGENT_DAYS}일 초과", "block"),
    (-LONG_URGENT_DAYS, -1, "긴급 · 납기 지남", "warn"),
    (0, 6, "이번 주", "warn"),
    (7, 13, "다음 주", "ok"),
    (14, 29, "2~4주", "ok"),
    (30, None, "30일 이상", "ok"),
]

# ── 검증 규칙 (Day1 실습 D) ───────────────────────────────────────
# **값을 코드에 박지 않는다.** validate.run_checks() 는 이 표를 읽어 판정만 한다.
# 임계·컬럼·판정이 바뀌면 고치는 곳은 여기 하나다.
#
#   판정을 가르는 질문은 하나 — 이 규칙이 깨진 채로 계산하면 값이 틀리는가?
#       틀린다 → block  /  해석만 조심하면 된다 → warn
VALIDATION = {
    # 규칙 1 — 행 수. 기준행 대비 비율로 본다(테이블 목록이 아니라 규칙으로).
    "행수": {
        "기준행": {            # 2026-09-05 적재 실측 (6공정 분해 후).
            "orders": 96_000, "order_events": 1_036_324, "defect_events": 94_466,
            "product_cost_bom": 1_200, "worklog_검사": 137_791,
            "experiments": 3, "experiment_assignments": 19_064,
        },
        "차단비율": 0.5,       # 기준의 50% 미만 → block
        "경고비율": 0.8,       # 기준의 80% 미만 → warn
        # ★ 2026-09-05 신설 — **늘어난 것도 본다.**
        #   전에는 줄어드는 쪽만 봤다. 그런데 6공정 분해로 order_events 가
        #   57만 → 104만(+81%)이 됐는데 검사가 **조용히 통과**했다.
        #   중복 적재로 두 배가 돼도 못 잡는다는 뜻이다 — 그러면 분모가 부풀어
        #   모든 비율이 틀리는데, 줄어들 때와 정확히 같은 크기의 사고다.
        #
        #   차단이 아니라 경고인 이유: 늘어난 것이 **중복인지 진짜 증가인지
        #   데이터만 봐서는 못 가른다.** 사람이 보고 판단할 자리다.
        "경고상한": 1.5,       # 기준의 150% 초과 → warn
        # ⚠️ 작은 표에는 안 건다. 3행짜리가 5행이 되면 167%지만 그건 중복 적재가
        #   아니라 그냥 작은 표다 — 2026-09-05 에 test_guards 가 이걸 잡아냈다.
        #   중복 적재는 **큰 표**에서 사고가 된다(분모가 부푼다).
        "상한적용최소": 1_000,
        "근거": "직전 적재 대비. 시스템 이관·추출 중단으로 파일이 잘려 오면 "
                "절반 밑으로 떨어진다. 분모가 조용히 줄어 모든 비율이 틀린다. "
                "반대로 중복 적재로 부풀어도 같은 크기의 사고다.",
    },
    # 규칙 2 — 필수 컬럼. 없으면 그 계산이 아예 불가능한 것만 적는다.
    "필수컬럼": {
        "컬럼": {
            "orders":           ["수주ID", "수주일", "납기일"],
            "order_events":     ["수주ID", "이벤트일", "이벤트구분"],
            "defect_events":    ["수주ID", "검사수량", "불량수량"],
            "product_cost_bom": ["제품도번", "판매단가", "총원가"],
            "worklog_검사":      ["수주ID", "생산일자", "생산수량"],
            "experiments":            ["experiment_id", "experiment_name"],
            # variant 가 없으면 대조군·처치군을 가를 수 없어 실험이 통째로 무의미해진다
            "experiment_assignments": ["experiment_id", "수주ID", "variant"],
        },
        "판정": "block",
        "근거": "키(수주ID)가 없으면 조인이 불가능하고, 날짜가 없으면 기간 집계가, "
                "수량이 없으면 어떤 비율도 못 낸다. 부록 A — 차단은 '키가 없는' 쪽이다.",
    },
    # 규칙 3 — 날짜 범위. **관측 컬럼만 본다.**
    "날짜범위": {
        "관측컬럼": {          # 이미 일어난 일 — 기간 밖이면 집계 분모가 흔들린다
            "orders": "수주일", "order_events": "이벤트일",
            "defect_events": "검사일", "worklog_검사": "생산일자",
            "experiment_assignments": "assigned_at",   # 배정 시점 = 수주일
        },
        "예정컬럼": ["납기일"],  # 앞으로 올 일 — 기간 밖이 **정상**이라 검사하지 않는다
        "차단비율": 0.05,       # 관측 행의 5% 초과가 기간 밖 → block
        "근거": "관측 데이터가 PERIOD 를 넘는 것은 대개 추출 시점 차이라 그 행만 빼면 "
                "된다(경고). 다만 5%를 넘으면 기간을 잘못 잡은 것이고 월 분모가 실제로 "
                "틀어진다(차단). 납기일은 미래여야 정상이므로 이 규칙에서 뺀다 — "
                "2026-08-29 기준 10,293건(10.7%)이 기간 밖인데 전부 아직 안 온 납기다.",
    },
    # 규칙 4 — 참조 무결성.
    #
    # ★ **이 규칙은 강사 레퍼런스에서 가져왔다.** validate.reference_checks_telecom()
    #   12건 중 7번이다. 교안이 *"읽고 내 데이터에 맞는 것만 가져다 고쳐 쓰라"* 던
    #   목록에서 우리에게 맞는 것 하나를 실제로 옮겨 온 것이다.
    #
    # 왜 필요했나 (2026-09-01) — 규칙 1~3만으로는 **"이름은 맞고 내용이 다른 파일"**을
    #   못 잡는다. 강사 배포본의 통신사 experiments.parquet(5행)을 우리 data/ 에
    #   넣어 보니 필수 컬럼도 있고 행 수도 기준의 167%라 **세 규칙을 전부 통과했다.**
    #   실험 ID까지 EXP-001~003 이 겹쳐서 통신사 가설로 우리 성과를 계산하게 된다.
    #   조인이 실제로 이어지는지는 **키를 맞춰 봐야만** 알 수 있다.
    "참조무결성": {
        "관계": [
            # (자식 테이블, 자식 키, 부모 테이블, 부모 키)
            ("order_events",           "수주ID",        "orders",           "수주ID"),
            ("defect_events",          "수주ID",        "orders",           "수주ID"),
            ("worklog_검사",            "수주ID",        "orders",           "수주ID"),
            ("experiment_assignments", "수주ID",        "orders",           "수주ID"),
            ("orders",                 "제품도번",       "product_cost_bom", "제품도번"),
            ("experiment_assignments", "experiment_id", "experiments",      "experiment_id"),
        ],
        # 부모에만 있고 자식이 한 건도 안 붙은 것 — 끊어졌다기보다 '아직 안 쓰임'이라 경고다
        "고아부모": [("experiments", "experiment_id",
                      "experiment_assignments", "experiment_id")],
        "차단비율": 0.01,
        "근거": "끊어진 참조는 조인에서 조용히 사라진다 — 에러가 안 나고 분모만 줄어 "
                "모든 비율이 틀린다. 규칙 1~3은 파일의 모양만 보므로 "
                "'이름은 맞고 내용이 다른 파일'을 통과시킨다. 키를 맞춰 봐야 걸린다. "
                "(강사 레퍼런스 검증 7번을 우리 키로 옮긴 것)",
    },
}

# ── 상태 색 ───────────────────────────────────────────────────────
# 의미가 고정된 규칙이다. **도메인이 바뀌어도 바꾸지 않는다.**
COLORS = {
    "ok": "#10b981",       # 통과 / 정상
    "warn": "#f59e0b",     # 경고 / 표본 부족 / 가드레일 악화
    "block": "#f43f5e",    # 차단 / 검증 실패 / 실험 무효
    "none": "#64748b",     # 데이터 없음 / 효과 없음
}
# 브랜드 색은 자유롭게 바꿔도 된다.
BRAND = {
    "primary": "#4f46e5",
    "ink": "#0f172a",
    "muted": "#64748b",
    "line": "#e2e8f0",
    "bg": "#f8fafc",
    "surface": "#ffffff",
}

# ── 리포트 ────────────────────────────────────────────────────────
# 8장 중 3장은 사람이 쓴다. **이 구분은 바꾸지 않는다.**
# 해석과 제안을 자동화하는 순간 책임이 사라진다.
REPORT_SECTIONS = [
    ("1. 요약", "auto"),
    ("2. 배경", "human"),
    ("3. 방법", "auto"),
    ("4. 결과", "auto"),
    ("5. 실험", "auto"),
    ("6. 해석", "human"),
    ("7. 한계", "auto"),
    ("8. 제안", "human"),
]

# 발송은 초안까지만. 실제로 전송하지 않는다.
EMAIL_ENABLED = False
EMAIL_TO_EXAMPLE = "team-growth@example.com"
