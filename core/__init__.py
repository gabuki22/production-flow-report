"""core — 계산과 규칙이 사는 곳.

이 파일 자체는 비어 있다. `__init__.py` 는 **이 폴더를 파이썬 묶음(패키지)으로
쓰겠다**는 표시라, 있어야 `from core import metrics` 가 된다.

    config   설정 하나 — 도메인을 바꿀 때 여기만 고친다
    load     데이터 적재
    metrics  계산 전부
    validate 검증 4종 (행 수·필수 컬럼·날짜 범위·참조 무결성)
    gates    게이트 3종 — 1·2는 되돌릴 수 있고 3은 못 한다
    todo     아직 안 채운 자리 표시 (골격 전용)
"""
