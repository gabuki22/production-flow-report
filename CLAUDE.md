# my-report2 — 성우산업 생산 흐름 리포트 앱 (실전본)

발주부터 출하·미납까지 **어디서 막히고 얼마나 새는지**를 실측해 리포트까지 내는 앱.

> 이 파일은 **새 대화가 처음 읽는 기준**이다. 여기 없는 것은 물어보고, 지어내지 않는다.
> 작성 2026-09-02 (8주차 Day2 실습 B-2) · 정한 것만 적었고 안 정한 것은 **미정**으로 뒀다.
>
> **폴더가 셋이다.** `mydomain/`=데이터·근거(정본) · `my-report/`=교안 진도 ·
> **`my-report2/`=여기, 실전본.** 서로 import 하지 않는다. → README.md

---

## ⚠️ 데이터는 전부 합성이다

실 ERP 반출이 되지 않아 **스키마와 값 체계만 복제**했다.
방법은 확인할 수 있으나 **판정은 실데이터로만** 할 수 있다.
여기 어떤 수치도 회사의 실제 실적이 아니다.

---

## 그레인 — 무엇을 하나로 세는가

**제품도번 1개 = 한 줄** (2026-09-12 개정). 어제까지는 `발주 1건(수주ID)` 이었다.

> ★ **바꾼 이유는 실무 쪽이다 — 실 ERP 에 수주 식별자가 없다.** 거래처·도번·수량만
> 있어 *"이 주문과 저 주문이 다른 건"* 을 구분할 키가 없다(`notes/11_그레인보정.md`,
> 2026-08-29). 합성에서는 `SO2601-0042` 를 우리가 붙였으므로 안 보였을 뿐이고,
> **제품도번은 실 ERP 에 그대로 있다.**
>
> ★ 바꿔도 전체 지표는 **0.02%p** 밖에 안 움직인다(발주 단위 90.64% / 도번 평균
> 90.66%). 도번당 발주가 38~88건으로 고르게 퍼져 있어서다. **기존 검산이 살아 있다.**
>
> ⚠️ **발주 건을 안 세는 것이 아니다.** 도번 안에서 발주를 세되 그 발주를 무엇으로
> 구분하느냐가 데이터마다 다르다 — 합성은 `수주ID`, 실 ERP 는 **대리 키**
> (고객사+도번+수량+접수일 · 정확도 99.9%).

도번 단위 집계는 `metrics.part_facts()` 다. 발주 단위 계산(`order_facts`)을
**재료로 쓴다** — 두 곳에서 따로 세면 언젠가 갈린다.

한 수주가 같은 단계를 두 번 밟을 수 있다 — 미납이 다음 주기 생산계획으로 되돌아온다.
그래서 **단계별 최초 도달만** 세고 재투입은 다시 세지 않는다.

| | |
|---|---|
| 답하려는 질문 | *"이 도번이 결국 제때 나가는가"* (한 건마다가 아니라 **결국**) |
| 세는 법 | **제품도번 단위** — 그 안의 발주는 단계별 최초 도달만 |
| 발주를 무엇으로 구분하나 | 합성은 `수주ID` · 실 ERP 는 **대리 키**(고객사+도번+수량+접수일) |
| 분모 | **완주 코호트** — 접수 후 61일이 지난 것만 |

**분자와 분모의 단위를 섞지 않는다.** 행/고유를 섞으면 앞 3단계가 100%를 넘는다
(발주접수 103.0%). 행/행으로 일관되게 세면 안 넘지만 *다른 질문*에 답하게 된다.

**코호트 61일의 근거** — 납기가 수주 + 25~60일이라 그 전에는 완주 여부를 판정할 수 없다.
안 자르면 최종 통과율이 95.6% → 85.6%로 **10.0%p 나쁘게** 나오는데,
그 차이는 실패가 아니라 **아직 진행 중인 것**이다.

---

## 퍼널 단계와 조건

```
발주접수 → 영업계획 → 생산계획 → 지시서작성
  → 사출 → 지그삽입 → 도장 → 레이저 → 인쇄 → 검사 → 출하
              ↑                                    │
              └──────── 미납확인 ←─────────────────┘
```

`order_events.이벤트구분` 값이 곧 단계 이름이다. 조건은 **그 값이 있는가** 하나뿐이다.

**미납확인은 단계가 아니다.** 퍼널 밖 — 순환의 입구다. 납기를 넘긴 건이 다음 주기
생산계획으로 되돌아온다. 그래서 `FUNNEL_STEPS` 에 넣지 않고 `STAGE_UNPAID` 로 따로 둔다.

**퍼널로 성립하는가** — 앞 단계를 건너뛴 수주 **0건**(`funnel_skips()`).
0이 아니게 되면 퍼널이 아니라 분류이므로 단계를 다시 짠다.

---

## 유지 퍼널 — 무엇의 유지인가

⚠️ **고객 유지가 아니라 수주 생존이다.** 우리 수주는 출하되면 거기서 끝난다.
통신사의 유지율과 같은 말로 읽으면 안 된다.

| 단계 | 조건 |
|---|---|
| 납기도래 | 완주 코호트 중 `납기일 ≤ 기준일` |
| **미납없음** | 위 + `미납확인` 이벤트가 **한 번도 없음** — 한 번에 통과한 것 |
| 납기내출하 | 위 + `출하일 ≤ 납기일` |

셋은 **중첩**이다(각각 앞 단계의 부분집합). 중첩이 아니면 퍼널이 아니라 분류다.
앞 단계를 안 거치고 나타난 수주 **0건** — 퍼널로 성립한다.

**이 퍼널의 값어치는 끝값이 아니라 손실 분해다.** 끝값(90.64%)은 정의상 납기 준수율과
거의 같다. 값어치는 그 숫자를 둘로 가르는 데 있다.

```
① 미납을 겪어 빠짐     4,753건 (6.4%)   ← 순환에 빠진 것
② 미납 없이도 못 지킴   2,214건 (3.0%)   ← 다른 이유
```

> ⚠️ 2026-09-03 개정 — 전에는 `출하완료` 가 2단계였는데 **4단계로 넣어 보니
> 마지막 구간이 100.0%** 였다(미납을 안 겪고 출하된 수주는 전부 납기 내).
> 아무것도 안 거르는 단계라 뺐다. 교안: *"전환율이 0%나 100%인 단계가 없는가."*

### ★ 이탈이 아니라 **긴급품**이다 (2026-09-02 개정)

**긴급품** = `납기일이 지났는데 아직 출하되지 않은 수주`. 유예는 **0일**.

현장 확인: *"납기가 지나면 최대한 빨리 납품하려고 노력해야함. 긴급품이 됨"*

통신사의 이탈은 고객이 **떠나서 사라지는 것**이지만, 우리 수주는 납기가 지나도
사라지지 않고 **더 급해진다.** 이름이 다르면 대응도 다르다 —
이탈은 되찾는 일이고, **긴급품은 먼저 내보내는 일**이다.

단계가 아니라 **아직 안 나간 자리**라 `RETENTION_STEPS` 에 넣지 않고 따로 센다.

- 유예 0 기준 **4.45%(3,311건)** — 유예를 두면 그만큼 줄어든다
- 늦게라도 출하된 건은 긴급품이 **아니다** → `지연 출하율`(4.63%) 로 따로
- 둘을 합치면 `1 − 납기 준수율` 과 같아져 지표가 겹친다

---

## 지표와 계산식

| 지표 | 계산식 | 분모 |
|---|---|---|
| **납기 준수율** (주지표) | 납기내출하 ÷ 납기 도래 | **납기 도래 전체** |
| **불량률** (가드레일) | 불량수량 합 ÷ 검사수량 합 | **수량 가중** |
| **긴급품 비율** | 긴급품 ÷ 완주 코호트 중 납기 도래 | 완주 코호트 |
| 지연 출하율 | 지연출하 ÷ 출하완료 | **출하된 건** |

### ★ 지표마다 확정되는 시점이 다르다 (2026-09-04)

**납기 준수율은 납기일이 지나는 순간 판정이 끝난다.** 긴급품·지연 출하는 아니다 —
납기를 넘긴 수주가 **납기 뒤 최대 24일**(중앙 13일)에 걸쳐 나가기 때문이다.

```
데이터 끝에 가까운 달   아직 안 나간 것 → 긴급품으로 세어 과대
                       늦게 나갈 것    → 지연 분모에서 빠져 과소
```

실측: 2026-08 이 긴급품 4.5%대 → **6.82%**, 지연 4.2% → **2.51%** 로 반대로 튀었다.
개선이 아니라 관찰 기간이 모자란 것이다. **둘이 반대로 움직이면 경계 효과를 의심한다.**

→ `config.DUE_SETTLE_DAYS = 25` (실측 최대 24일 + 1). 안 지난 달은 `monthly()` 가
**값을 아예 안 남긴다.** 화면은 그 지표의 비교 구간을 따로 적고 이유를 띄운다.

### 분해는 무엇을 쪼개느냐가 축보다 크다 (2026-09-04)

같은 축·같은 데이터인데 보는 값에 따라 격차가 이만큼 갈린다.

```
퍼널 전환율   최대 0.29%p (검사→출하)       →  "이 축으로는 안 갈린다"
납기 준수율   4.18%p (D사 87.41 / C사 91.59) →  "D사를 봐야 한다"
```

> 값 갱신 2026-09-08(Day1 프롬프트 5). 종전 `0.63%p (공정작업→출하)` · `4.24%p (88.3/92.5)`
> 는 **09-05에 퍼널을 6공정으로 쪼개기 전** 숫자다 — 병목 구간 이름이 `공정작업→출하`
> 에서 `검사→출하` 로 바뀌었는데 이 줄이 안 따라왔다. **구조를 바꾸면 그 구조로 잰
> 숫자가 전부 낡는다.**

**우리 병목은 새는 것이 아니라 밀리는 것**이라 탈락률에는 안 나타난다.
→ `metrics.metric_by(t, dim)` · 대시보드 기본 기준은 **납기 준수율**.

⚠️ 축별 차이는 **생성기에 심은 값**이다(`LATE_MULT_*`). 발견이 아니다.

**납기 준수율의 분모를 '납기 도래 전체'로 둔 이유** — 출하한 건만 세면 미납이 분모에서
빠져 **어려운 건을 미룰수록 지표가 올라간다**(굿하트).

**분모를 바꾸면 같은 지표가 이렇게 갈린다.**

```
출하·납기있음 94.9%  >  납기 도래 90.6%  >  출하 전체 88.2%  >  전체 수주 84.3%
```

순서가 단조롭지 않은 이유는 분모 크기가 아니라 **결측 처리**다 —
납기 미정 7.0%가 `NaN` 비교로 미준수에 깔린다. **결측(6.7%p)이 분모 선택(4.6%p)보다 크다.**

**지표 정의의 원본은 위키다.** 이 앱은 옮겨 적은 것이지 여기서 새로 정의하지 않는다.

---

## 임계값과 근거

| 지표 | 경고 | 위험 | 방향 | 근거 |
|---|---|---|---|---|
| 납기 준수율 (현재 **90.5%**) | 90 | 80 | 낮으면 나쁨 | 현장 기준 — *"납기는 고객과의 약속이라 90%는 지켜져야 하고, 80%를 밑돌면 거래가 위태롭다"* |
| 불량률 (현재 **3.54%**) | 4 | 5 | **높으면 나쁨** | 현장 감각 *"보통 2~3%"* — 평상시(2.50%)와 스파이크 포함(3.56%)을 가른다 |
| 긴급품 비율 | 10 | 10 | **높으면 나쁨** | 현장 기준 2026-09-02. **한 줄 규칙** — 10%를 넘으면 바로 위험(경고 구간 없음). 월 실측 4.32~6.96%라 지금은 어느 달도 안 걸린다 = 큰 사고만 잡는 선 |
| 지연 출하율 | 10 | 10 | **높으면 나쁨** | 위와 같음. 월 실측 0.27~4.77% |

**통계적 변동폭이 아니라 사업이 감내할 수 있는 선**에서 그었다.
교안이 든 근거 4종 중 **"조직의 합의"**.

**방향이 반대인 지표**(불량률·긴급품 비율)는 부등호가 뒤집힌다 → `metrics.status_of()` 의
`higher_is_worse`. 안 적으면 색이 반대로 뜬다.

| 그 밖 | 값 | 근거 |
|---|---|---|
| `MIN_SAMPLE` | **100** | 월 분모 평균 6,582건. 100이면 월 단위 판정이 되고 더 쪼개면 못 쓴다. **교안 기본 1,000도, Day2 권고 30도 쓰지 않는다** — 30은 "연 수백 건 도메인" 전제라 우리에게 안 맞는다 |
| `COHORT_DAYS` | 61 | 납기가 수주 + 25~60일 |
| `URGENT_GRACE_DAYS` | **0** | 납기 다음 날부터 긴급 (현장 기준) |
| `LONG_URGENT_DAYS` | 30 | 오래 밀린 긴급품을 가르는 선 |
| `MIN_EXP_DAYS` | **28** | 현장 기준 2026-09-03 — *"새 절차를 넣으면 2주면 긴장이 풀린다"* → 풀린 뒤로도 2주를 더 봐야 평상시가 나온다. 전에는 표본에서 역산한 41일이었으나 **현장 값이 아니었다** |
| `EFFECT_MIN_PP` | **1.5** | 현장 기준 2026-09-03 — 스파이크 미발생률이 **아무것도 안 해도** 월 |Δ| 중앙 1.51%p 흔들린다. 그보다 작은 차이는 우연과 구분되지 않는다 |
| `GUARDRAIL_TOLERANCE` | **0.03** | 현장 기준 2026-09-03 *"느슨하게"* — 납기 준수율 월 |Δ| 중앙 0.40%p의 7.5배라 **진짜 악화만** 잡는다. 전에는 가설 문구에서 가져온 2%p |

---

## 못 믿을 조건과 판정 순서 (2026-09-03 Day3)

**조건은 셋인데 분기는 하나다.** 셋 다 결론이 같다 — *이 숫자로는 판단할 수 없다.*
조건이 셋이라고 분기를 셋으로 만들면 나중에 하나를 빠뜨린다. → `metrics.trust_check()`

| 조건 | 기준값 | 근거 |
|---|---|---|
| 표본이 모자란다 | `MIN_SAMPLE` **100건** 미만 | 분해 칸이 2,583~29,649건이라 어느 후보로도 안 감춰진다. 100은 실험 하나(64건)를 정확히 거른다 |
| 기간이 안 찼다 | `MIN_EXP_DAYS` **28일** 미만 | *"새 절차를 넣으면 2주면 긴장이 풀린다"* → 풀린 뒤로도 2주 더 봐야 평상시가 나온다 |
| 배정이 공정하지 않다 | SRM **p < 0.001** | 통계 관례값. 도메인과 무관해 골격 그대로 쓴다 |

**하나라도 걸리면 사유를 돌려주고 지표를 계산하지 않는다.** 걸린 사유는 **전부** 돌려준다 —
첫 하나만 돌려주면 그것만 고치고 다시 돌렸을 때 또 막혀 두 번 헛걸음한다.

### 판정 순서 — 통과한 것만

```
1. 주지표가 EFFECT_MIN_PP(1.5%p) 이상 움직였는가   ← 아니면 "효과 없음"
2. 가드레일이 GUARDRAIL_TOLERANCE(3%p) 넘게 나빠졌는가  ← 그러면 "주의 필요"
3. 둘 다 통과                                      ← "성공"
```

**2번을 1번 뒤에 둔다.** 주지표가 좋으면 거기서 멈추고 싶어지는데 코드는 멈추지 않는다.
`p값`은 *"우연이 아니다"* 이지 *"의미 있게 크다"* 가 아니라 **둘을 함께** 본다.

| 판정 | 색 | 언제 |
|---|---|---|
| 성공 | `ok` | 주지표 개선 + 가드레일 이상 없음 |
| **주의 필요** | `warn` | 주지표 개선 + **가드레일 악화** ← 좋아 보이는데 좋지 않은 것 |
| 효과 없음 | `none` | 크기가 작거나 우연과 구분 안 됨 |
| 무효 | `block` | 못 믿을 조건에 걸림 |

---

### 판정 과정을 기록으로 남긴다 (2026-09-04)

`experiment_results()` 는 판정과 함께 **어느 물음에서 갈렸는지**를 `steps` 로 남긴다.
화면은 `ui.verdict_steps()` 로 펼치기만 한다(계산하지 않는다).

**묻지 않은 물음은 `passed=None` 으로 남긴다.** 안 남기면 통과한 것으로 읽힌다 —
*"가드레일 이상 없음"* 과 *"가드레일을 보지도 않았음"* 은 화면에서 구분되지 않는다.

⚠️ `passed` 는 반드시 파이썬 `bool` 로 눌러 담는다. `numpy.bool_(False) is False` 는
**False** 라, 그대로 두면 화면의 분기가 조용히 안 걸린다. 값도 판정도 맞고 라벨만
비어서 눈으로는 못 찾는다. → `test_metrics.py` 8-4절

---

## 감추기와 설명하기는 같이 간다 (2026-09-04 Day3)

| 어디에 | 무엇을 |
|---|---|
| 카드 | **한 문장**만 — 사유로 카드를 덮으면 아무도 안 읽는다 |
| `@st.dialog` "감춘 근거" | 배정 비율·SRM p·표본·기간 **전부** |
| `st.popover` "정의" | `config.METRIC_DEFS` — 식·그레인·기준일·주의 |
| `st.status` "판정 과정" | 물음 4개와 어디서 갈렸는지 |

**요약만 두면 사람이 규칙을 우회한다.** 교안: *"이유를 모르면 우회하려 한다."*

지표 정의의 **정본은 `notes/03_지표정의.md`** 다. `METRIC_DEFS` 는 화면용 요약이고
값이 갈리면 노트가 이긴다. 두 곳에서 고치게 되므로 **늘리지 않는다.**

---

## 게이트 (2026-09-04)

**근거 없이 통과시킬 수 없다** — `config.GATE_NOTE_MIN = 10`.
게이트는 앱이 판단하지 않는 자리라 **사람이 무엇을 봤는지가 그 자리의 유일한
산출물**이다. 비워 둘 수 있게 만들면 반드시 비어 있다(9/3 기록 2건이 그랬다).

아카이브는 게이트별로 **나란히** 보여준다. 묻는 것이 다른 두 판단을 한 줄로
이어 붙이면 한 번의 판단으로 읽힌다.

---

## URL 에 실리는 상태 (2026-09-04)

`dim` · `basis` · `from` · `to` 네 가지. `ui.url_state()` / `ui.push_url()`.

⚠️ **URL 은 사람이 고칠 수 있는 입력이다.** 남이 보낸 링크의 축 이름을 그대로
`groupby` 에 넘기면 죽는다. **허용 목록으로 걸러 기본값으로 되돌린다.**
→ `test_guards.py` 10절

---

## 문장으로 내보낼 때 (2026-09-04 Day4)

**가르는 질문 하나** — *이 문장이 틀렸을 때 누가 책임지는가?*

| 자동 5장 | 사람 3장 |
|---|---|
| 1 요약 · 3 방법 · 4 결과 · 5 실험 · 7 한계 | **2 배경 · 6 해석 · 8 제안** |

**해석과 제안을 자동화하는 순간 책임의 주체가 사라진다.**
비어 있으면 `(작성되지 않음)` 으로 찍는다 — 채우면 아무도 빈 줄 모른다.

### 인과 단정 표현

`BANNED` **12개** — 교안 기본 여섯 + 제조·품질 여섯(`원인이다`·`유발했`·`기인한`·
`초래했`·`탓에`·`영향을 미쳤`). **말마다 왜 금지인지 주석을 단다** — 근거 없이
늘어난 목록은 나중에 누가 통째로 지운다.

★ **사람이 쓴 장에도 건다.** 2026-09-04 이전에는 `kind=="auto"` 갈래에서만 돌아
**2·6·8장이 아예 검사되지 않았다.** 교안이 지목한 대로 사람이 쓴 해석에
"때문에"가 훨씬 자주 들어가는데, 가장 필요한 곳이 비어 있었다.

### 한계 절 — 조립한다

세 곳에서 온다: **검증 경고** · **못 한 것** · ★ **찾았는데 없던 것**.
세 번째가 늘 빠진다. *"없었다"도 결과*라 안 적으면 다음 사람이 또 찾는다.

**무조건 들어가는 문장 셋** (조건부 항목보다 **앞에** 둔다 — 사이에 섞으면
언젠가 조용히 없어진다)

```
관측 데이터이므로 인과를 주장할 수 없다
기간이 config.PERIOD 이므로 그보다 긴 주기의 변화는 관측되지 않는다
데이터가 전부 합성이다
```

`st.data_editor` 로 표에서 고칠 수 있다(`limit_rows`/`limits_from_rows`).
**검증 경고를 빼면 화면에 흔적을 남기고**, 위 셋은 체크를 꺼도 되살린다 —
합성 데이터 문장이 빠진 문서는 실측 보고서로 읽힌다.

### 게이트 3 앞 — 발송 전 점검 5항목

`sections.preflight()`. **하나라도 걸리면 확인 문구 칸조차 열지 않는다.**
사람이 매번 기억해서 하는 점검은 언젠가 건너뛴다.

```
1 사람이 쓰는 장이 채워졌는가
2 인과 단정 표현이 없는가 (자동 5 + 사람 3 전부)
3 검증 경고가 한계 절에 다 있는가
4 감춘 항목의 수치가 새지 않았는가   ← 무효 실험 주변만 본다
5 요약의 지표가 화면과 같은가
```

⚠️ **유출은 감춘 것 주변만 본다.** 문서 전체에서 숫자를 찾으면 *감추지 않은*
항목의 값이 걸려 오탐이 난다(2026-09-04 실제로 헛짚었다).
그리고 **조건 값**(표본·기간·배정 비율)은 보여주는 쪽이다 — 감출 것은 **지표 값**.

게이트 3에도 `GATE_NOTE_MIN` 을 건다. 가장 되돌릴 수 없는 자리인데
전에는 근거가 코드에 박힌 자동 문구였다.

---

## 제안서 절 목록 (2026-09-10 Day3 · 여기부터 고친다)

**읽는 사람은 생산본부 팀장·임원**이고, 읽고 나서 **승인 / 조건부 승인 / 보류** 중
하나를 결정해야 한다. 그 사람이 결정하는 데 필요한 순서로 배열했다.

> ★ **절의 순서가 곧 설득의 순서다.** 규모(3)를 제안(4) 뒤에 두면 *"왜 이걸 해야 하지"*
> 가 안 풀린 채로 제안을 읽는다. 위험(5)을 요청(6) 뒤에 두면 **결정한 다음에 리스크를
> 듣는다.** 순서를 바꾸지 않는다.

| # | 절 제목 (질문형) | 이 절이 답하는 질문 | 채우는 데이터 함수 | 누가 |
|---|---|---|---|---|
| 1 | **지금 어디서 빠지고 있습니까** | 무슨 일이 일어나고 있나 | `metrics.funnel()` → `topic_evidence()["현황"]` | 자동 |
| 2 | **어느 쪽에서 벌어지고 있습니까** | 어디서 벌어지나 | `metrics.metric_by()` → `topic_evidence()["원인"]` | 자동 |
| 3 | **연간 몇 건짜리 문제입니까** | 얼마짜리 문제인가 | `topic_evidence()["규모"]` + `["추세"]` | 자동 |
| 4 | **무엇을 하자는 것입니까** | 그래서 뭘 하자는 건가 | `proposal.parse_cards()` (제안카드.md) | 자동 |
| 5 | **이 판단이 틀릴 수 있는 지점** | 틀리면 어떻게 되나 | — (사람이 쓴다) | **사람** |
| 6 | **무엇을 결정해 주셔야 합니까** | 나한테 뭘 해달라는 건가 | 문장은 사람 · 규모·선택지는 `topic_evidence()["규모"]` | **사람** |

**여섯 개다.** 최대 7을 안 넘고, **사람이 쓰는 절은 정확히 둘**이다.
셋 이상이면 자동화가 덜 된 것이고, 하나도 없으면 책임질 사람이 없는 문서다.

### 만들지 않는 절 — 채울 함수를 못 댄다

**자리를 비워 두는 것과 자리를 안 만드는 것은 다르다.** 나중에 채우겠다고 만들어 두면
그 자리가 **그대로 인쇄된다.**

| 만들지 않는 절 | 왜 |
|---|---|
| ~~**얼마짜리 금액입니까**~~ | ✅ **2026-09-11 해소 — 이 줄이 틀렸다.** `orders` 에 `수량`·`단가` 가 있어 **조회하면 나온다**(`metrics.amount_by()`). *"역산한 값이라 계약 단가가 아니다"* 는 **정확도 경고**였는데 그것을 *없다* 로 읽고 절을 아예 안 만들었고, **결재자가 보류한 자리가 정확히 거기였다.** 한계(역산값 · 건당 평균 환산)는 문서에 함께 적는다 |
| **왜 그런 일이 생겼습니까** (원인 규명) | **이 도메인에는 없음.** 착수 지연일·자재 결품 이력·계획 변경 횟수가 데이터에 없다. *어느 칸이 낮은가*까지는 대지만 *왜 낮은가*는 못 댄다. 2절 제목을 *"왜"* 가 아니라 **"어느 쪽에서"** 로 둔 이유다 |
| **시험해 보니 어땠습니까** (실험 결과) | **이 도메인에는 없음.** 실험 셋 중 둘이 무효(배정 깨짐·표본 부족)고 하나는 효과 없음이다. 무효를 근거로 쓰면 감춘 의미가 사라진다 |
| **다른 회사는 어떻습니까** | **이 도메인에는 없음.** 비교할 데이터가 없다 |

### 절을 추가·삭제할 때의 기준

> **"이 절을 채울 데이터 함수 이름을 지금 댈 수 있는가?"**
> 못 대면 그 절은 **없는 것**이다.

- 절 제목은 **우리 결재 문서에서 쓰는 말**로. 퍼널·코호트·가드레일·그레인 같은
  수업 용어를 문서에 내지 않는다 → `config.PROPOSAL_WORDS` 한 곳에서만 바꾼다
- 절 제목은 **질문형**이다. 리더가 던질 질문을 그대로 제목으로 쓰면
  그 아래 문단이 자동으로 답이 된다
- **사람이 쓰는 절은 둘 — 위험·철회 기준 / 요청.** 늘리지 않는다

### 그래프 규칙

| 절 | 본문의 주장 | 그래프 |
|---|---|---|
| 1 현황 | *"여기서 가장 많이 빠진다"* | 단계별 막대 — **병목 하나만** 강조색 |
| 2 원인 | *"이 집단과 저 집단이 다르다"* | 축별 가로 막대 — **최고·최저만** 색, 나머지 회색 |
| 3 규모 | *"줄고 있다 / 늘고 있다"* | 추세 꺾은선 — 임계선은 점선 |

- **그림 하나에는 그것이 증명하는 문장 하나**가 본문에 있어야 한다. 없으면 장식이다
- **색은 셋까지** — 강조 1 · 기본 1 · 회색 1
- ⚠️ 우리 축 분해는 **87~92% 구간에 몰려 있다.** 0부터 그리면 막대 길이비가 95%를
  넘어 차이가 안 보인다 — **눈금을 자르되 잘랐다는 것을 축에 표시한다**

---

## 코드를 고칠 때 지킬 것

1. **지표 정의는 `core/config.py` 에서만 바꾼다.** 임계·컬럼·규칙이 표에 있고 코드는 판정만 한다.
2. **계산 경로에 현재 시각을 넣지 않는다.** 기준일은 `config.TODAY` 고정.
   같은 입력에 같은 결과가 나와야 보고에 쓸 수 있다.
   (허용된 예외 **2곳** — `report/sections.py` 의 이메일 초안 footer,
   그리고 `pages/3_리포트.py` 의 PDF **파일 이름**. 둘 다 표시용이라 숫자에 영향 없다.
   파일 이름에 시각을 넣는 것은 되지만 그 시각으로 무엇을 계산하지는 않는다)
3. **시키지 않은 파일은 고치지 않는다.**
4. **계산은 `core/metrics.py` 에만.** 화면(`pages/`)에서 합계·비율을 만들지 않는다 —
   같은 수치를 리포트에서 다시 셀 때 값이 갈린다.
5. **색·간격은 `config.COLORS`/`BRAND` 와 `viz/ui.py` 에서만.** 판정 색은 판정에만 쓴다.
6. **해석과 제안은 자동 생성하지 않는다.** 리포트 2·6·8장은 사람이 쓰는 자리다.
7. **원본 데이터(`data/`)는 읽기만 한다.** 고칠 일이 있으면 파생을 만든다.
8. **판정 순서를 바꾸지 않는다** — 주지표 크기 → 가드레일 → 성공.
   1번을 통과했다고 바로 성공으로 가면 **가드레일이 무너진 실험이 성공으로 보고된다.**
   `p값`은 *"우연이 아니다"* 이지 *"의미 있게 크다"* 가 아니다. **둘을 함께 본다.**
9. **못 믿을 때는 계산하지 않는다** — 계산해 놓고 숨기는 것이 아니다.
   `trust_check()` 를 지표 계산 **앞**에 두고 걸리면 거기서 끝낸다.
   계산해 놓으면 값이 변수에 들어 있어 **리포트·로그로 샌다.**
   *손에 없으면 못 쓴다. 그게 설계다.* → `test_metrics.py` 8-2절이 이 구조를 검사한다.
10. **감춘 항목에는 사유만 적고 수치는 적지 않는다** — 화면이든 문서든.
    사유에는 **실제 숫자**를 넣는다: *"표본 부족"* ✕ → *"표본 64건 (최소 100)"* ○.
    다만 **조건 값**(표본 수·기간·배정 비율)은 보여주는 쪽이다 — 감출 것은 **지표 값**
    (비교·증감·성과 p값·신뢰구간)이다.

11. **화면에만 있는 규칙은 계산 검사로 안 잡힌다.** 버튼 잠금·색·라벨 같은 것은
    `test_guards.py` 에서 **소스를 읽어** 확인한다.
12. **검사기를 만들면 그 검사기를 꺼 본다.** 규칙을 빼면 실제로 FAIL 하는지 확인하고
    되돌린다. 검사기와 대상이 같은 전제를 공유하면 **둘이 같이 망가지고 둘 다 통과한다.**
13. **정확히 0 은 대개 버그 신호다.** 격차 0.00%p 가 나오면 진짜 0인지 먼저 확인한다
    (2026-09-04: 없는 단계 이름을 넘겨 0이 나왔다).

14. **`report/proposal.py` 는 `report/sections.py` 와 같은 자동/사람 분리 원칙을 따른다**
    (2026-09-09 Day2). 가르는 질문은 *"이 절의 내용이 데이터를 다시 조회하면 똑같이
    나오는가"* — 그렇다면 자동, 아니면 사람이다. **판단이 들어가는 절을 자동화하면
    책임의 주체가 사라진다.**
    · ⚠️ **2026-09-10 개정** — 어제는 7절(auto 5 · human 2)이었으나 오늘 **6절
      (auto 4 · human 2)** 로 줄였다. 「적용」 절을 없앤 것이 아니라 **제안 절이
      그 일을 한다** — 카드를 분류대로 나열하는 것이 곧 후보 나열이라 절이 둘일
      까닭이 없었다. 절 목록의 정본은 `config.PROPOSAL_SECTIONS` 다.
    · **문장 검사(`check_phrasing`)와 `BANNED` 를 새로 만들지 않고 `sections.py` 것을
      그대로 import 한다.** 우리 BANNED 는 교안 기본 여섯 + 제조·품질 여섯이라
      새로 만들면 뒤의 여섯이 조용히 사라진다.
    · **`sections.py` 를 고치지 않는다.** 리포트(8장)와 제안서(6절)는 다른 문서다.
    · **절 순서를 바꾸지 않는다** — 하지 말 것 → 다시 할 것 → 할 것.
      "할 것"을 앞에 두면 읽는 사람이 거기서 멈추고 **멈추자는 제안은 안 읽힌다.**

15. **자동 절에 카드에 없는 값을 채워 넣지 않는다.**
    채워서 보여주면 그럴듯해 보이지만 **아직 안 정한 것을 정한 것처럼** 만든다.
    세어서 알리되 채우지 않는다.
    · ⚠️ **2026-09-10 개정 — 빈 자리를 `todo` 로 남기지 않는다. 그 절을 아예
      안 만든다.** 어제는 `"todo — 왜"` 를 넣고 화면이 `class="todo"` 를 붙였는데,
      **자리를 비워 두는 것과 자리를 안 만드는 것은 다르다** — 비워 두면 그
      자리가 결재 문서에 그대로 인쇄된다. 자동 절은 문장이 없으면 절이 없다.
    · **다만 사람이 쓰는 절은 반대다.** 비어도 절을 지우지 않고
      `(작성되지 않음)` 으로 찍는다 — 조용히 빼면 읽는 사람은 **위험을 따지지
      않은 문서**인 줄 모른다. 리포트 8장과 같은 방식이다.
    · **화면은 `제안카드.md` 를 읽기만 한다.** 화면에서 값을 고치기 시작하면
      카드와 화면 중 어느 것이 진짜인지 알 수 없게 된다.
    · ⚠️ **파서의 필드 목록을 카드와 함께 늘린다.** 카드에만 칸을 넣고 목록을 안 고치면
      파서가 조용히 못 읽고 화면은 *"값이 없다"* 고 정직하게 말한다 — 카드에는 있는데.
      2026-09-09 에 `크기` 칸으로 실제로 겪었다.

16. **폰트가 못 그리는 글자는 어느 매체에서도 쓰지 않는다** (2026-09-09).
    `「」`(U+300C/D) · `←`(U+2190) · `✓` · `⚠` · `★` 는 `NotoSansKR` 에 **없다.**
    PDF 에서 조용히 네모로 나가는데 fpdf2 는 경고를 stderr 로만 흘려 화면에서는 안 보인다.
    **매체마다 글자를 바꾸지 않는다** — 한 본문이 매체마다 달라지면 어느 쪽이 원본인지
    알 수 없다. → `to_pdf.missing_glyphs()` 가 PDF 만들기 2단계에서 세어 보여준다.

17. **요청 절의 문장은 사람이 쓴다. 자동으로는 재료만 붙인다** (2026-09-10 Day3).
    붙는 것은 셋 — **규모(연 N건)** · **선택지 셋**(승인 / 조건부 승인 / 보류, 각각에
    무엇이 따라오는지 한 줄) · **미뤘을 때 다음 분기까지 쌓이는 양**(연간 ÷ 4,
    그 가정을 괄호로 적는다). **무엇을 해달라는 말은 짓지 않는다** — 요청은 사람이
    책임지는 말이라 자동으로 만들면 책임의 주체가 사라진다.
    · **사람 문장이 맨 뒤**다. 문서의 마지막 줄이 결정을 요구하는 문장이어야
      읽은 사람이 무엇을 해야 하는지 알고 문서를 닫는다.
    · 결정 동사(**승인 · 결정 · 판단**)가 없으면 **화면에 경고만 띄우고 저장은
      막지 않는다.** 사람이 쓴 문장 때문에 저장이 막히면 사람이 문장을 안 쓴다.
      자동 절에 거는 검사는 자동으로 쓴 부분에만 건다.
    · ⚠️ **어디까지가 사람 말인지 절이 직접 들고 있는다**(`_sec(..., 사람=)`).
      글자를 보고 가르면 사람이 *"승인 —"* 으로 시작하는 문장을 쓴 순간 어긋난다.

18. **모르는 것은 낱말 하나로 두지 않는다. 셋으로 적는다** (2026-09-10 Day3).
    **[무엇을 모르는가] — [누가·어떻게 확인하는가] — [모르는 채로 할 수 있는 결정]**
    · 앞의 둘은 **`제안카드.md` 의 `비용`·`확인` 칸**에서 읽는다. 셋째는
      **선택지 셋 자체가 답한다** — 조건부 승인이 바로 그 자리다.
    · ★ **확인 주체를 지어내지 않는다.** 카드에 없으면 사람에게 묻고, 못 물었으면
      *"확인할 곳을 아직 정하지 않았습니다"* 라고 적는다. *"현업이 산정한다"* 같은
      두루뭉술한 말로 메우면 **아무도 자기 일인 줄 모른다.**
      (2026-09-10 에 실제로 그렇게 나갔다 → 레이저팀 · 생산관리 · 영업팀으로 확정)

19. **내보내기 전에 여섯 항목을 센다. 고치지는 않는다** (2026-09-10 Day3).
    `proposal.self_check()` — **빈칸 0 · 정체불명 용어 0 · 대응 문장 없는 그래프 0 ·
    마지막 줄 결정 동사 있음 · 절 7 이하 · 폰트에 없는 글자 0.**
    · 검사는 **내보내기 앞**에 둔다. 뒤에 두면 내려받고 나서 보게 되고 그때는 이미
      나간 뒤다.
    · **정체불명 용어 목록은 `config.PROPOSAL_JARGON` 한 곳**에 둔다. 코드에 박으면
      말이 늘 때 한 곳을 빠뜨리고, 빠뜨린 자리는 문서에 나가서야 보인다.
    · ⚠️ **그림이 무엇을 그리는지는 그림을 그린 그 표에서 뽑는다.** 주제 딕셔너리에서
      뽑으면 안 된다 — 축 주제의 `구간` 칸에는 구간이 아니라 **지표 이름**이 들어
      있어서 현황 절에 엉뚱한 이름을 씌운다(2026-09-10 오탐).
    · ⚠️ **못 찾은 것을 통과로 세지 않는다.** 그림은 있는데 대상을 못 뽑으면
      검사가 눈이 먼 것이지 문서가 옳은 것이 아니다 — 규칙 12대로 꺼 보다 드러났다.

20. **"이 도메인에는 없다"고 적기 전에 조회해 본다** (2026-09-11 Day4).
    *"얼마짜리 금액입니까"* 를 **만들지 않는 절**로 두고 한 달을 갔는데, 데이터에
    `수량`·`단가` 가 처음부터 있었다. 근거로 삼은 문장은
    *"역산한 값이라 계약 단가가 아니다"* — **정확도 경고**였지 없다는 뜻이 아니었다.
    · **한계와 부재는 다르다.** *"정확하지 않다"* 를 *"없다"* 로 읽으면 낼 수 있는
      것을 안 내게 되고, **그 자리에서 결재가 막힌다**(임원 보류 사유가 바로 그것).
    · 절을 만들지 않기로 정할 때는 **컬럼 목록을 실제로 훑은 뒤**에 적는다.
    · ⚠️ **수량·단가는 `int16` 이다. 그대로 곱하면 조용히 감긴다** —
      `300 × 772` 가 음수로 뒤집혀 건당 383원이 나왔다(실제 674,917원). 오류가
      안 나고 값만 틀린다. `astype("int64")` 뒤에 곱한다 → `metrics.amount_by()`

21. **밖으로 나가는 문서와 소스는 감추는 것이 다르다** (2026-09-11 Day4).
    · **회사명** — 소스에는 **일부러 남긴다**(근거가 *"현장 기준 구술"* 이라는
      사실이 지워지면 그 숫자를 못 믿는다). **나가는 문서에서만** 뺀다 →
      `config.DATASET_PUBLIC` · `proposal.self_check()` 의 「나가면 안 되는 말」.
    · **개인 이름** — 2026-09-11 에 **빼기로 한 것을 되돌렸다**(기쁨 결정).
      09-05 에 71건을 치환했으나 오늘 기준이 바뀌었다.
    · ⚠️ `config.DATASET` 자체를 바꾸면 안 된다 — `gates` 가 그 값으로 실행 기록을
      묶어서, 바꾸는 순간 **저장해 둔 사람 절을 못 불러온다.**

**바꾸기 전에 돌린다**

```bash
py -X utf8 tests/test_metrics.py    # 숫자 검산 (8/29 실측 대조표 포함)
py -X utf8 tests/test_guards.py     # 망가뜨린 데이터를 거르는가
py -X utf8 tests/test_app.py        # 화면·게이트·PDF 까지 조작
```

---

## 파일이 하는 일

| | |
|---|---|
| `core/config.py` | **도메인을 바꿀 때 고치는 곳은 여기 하나** |
| `core/load.py` | 데이터 적재 — 모드가 갈리는 유일한 파일 |
| `core/metrics.py` | 계산 전부 |
| `core/validate.py` | 검증 4종 (행 수·필수 컬럼·날짜 범위·참조 무결성) |
| `core/gates.py` | 게이트 3종. 1·2는 되돌릴 수 있고 3은 못 한다 |
| `report/sections.py` | 리포트 8장 (auto 5 · human 3) |
| `report/proposal.py` | 제안서 6절 (auto 4 · human 2) — `제안카드.md` 를 읽어 조립·HTML 내보내기 |
| `viz/proposal_charts.py` | 제안서 그림 3종 (이탈 막대 · 축별 막대 · 추세) |
| `수업자료/` | **강사 배포 템플릿(원형)** — 참고만 한다. ⚠️ **2026-09-10 부터 코드가 이 템플릿을 읽지 않는다** (`to_html()` 이 직접 만든다). 두 방식을 함께 두면 어느 것이 진짜인지 알 수 없다. **공개 사본에서는 뺀다**(`배포제외.txt`) |
| `pages/1~6` | 실행 · 대시보드 · 리포트 · 아카이브 · **납기 전망**(우리 화면) · **제안서**(2026-09-10 신설. 리포트에서 떼어 독립 메뉴로) |
| `DESIGN.md` | 강사 명세 원본 |
| `판단기준.md` | 게이트에서 사람이 무엇을 보고 판단하는가 |
| **`파일지도.md`** | **어떤 파일이 무엇을 하는가** — 손으로 안 적는다. `py -X utf8 파일지도.py --write` |

> ★ **파일 지도는 코드에서 뽑는다** (2026-09-04 강사 코멘트 대응).
> *"개발 전공이 아니면 어떤 파일이 무엇을 하는지 따로 공부해야 한다"* —
> 강사는 VS Code 로 파일 트리를 늘 보지만 우리는 Claude Code + 옵시디언이라
> 안 보인다. 그래서 문서로 갖되 **손으로 적지 않는다** — 손으로 적은 지도는
> 파일이 늘 때 반드시 어긋나고, **어긋난 지도는 없는 것보다 나쁘다.**
> 각 `.py` 맨 위 설명을 그대로 읽어 만들고, 설명 없는 파일이 있으면 `exit 1`.

---

## 미정 — 아직 안 정했다

아래는 **정하지 않은 것**이다. 채우려면 물어봐야 한다. **지어내지 않는다.**

| 무엇 | 왜 안 정했나 |
|---|---|
| ~~재작업 회수율~~ | ✅ **해소(2026-09-02)** — 현장 확인 *"불량이 나오면 대부분 폐기함"* → 회수율 ≈ 0%. 불량 손실은 **이익의 21%** 쪽이 맞다 |
| **판매단가 절대수준** | 재료비 ÷ 40%로 역산한 값이지 계약 단가가 아니다. **금액을 못 내는 것이 아니라 자릿수·상대 크기만 쓸 수 있다는 뜻이다**(2026-09-11 정정) — 문서에 낼 때 그 한계를 함께 적는다 |
| ~~공정별 분해~~ | ✅ **해소(2026-09-05)** — 6공정으로 쪼갰다. 다만 **공정별 체류일은 실측이 아니라 배분값**이다: 작업일보 개당 CT 비중으로 나눴는데 **CT 는 처리시간이지 대기시간이 아니다.** 줄 선 시간은 작업일보에 안 남는다 → 아래 새 미정 |
| **긴급품 선행지표** | 착수 지연일·자재 결품 이력·계획 변경 횟수가 없다. **사후 판정만** 된다 (9/2에 이탈→긴급품으로 바꿨는데 이 줄만 옛 이름이었다) |
| **확정 대기일 25일이 실제로도 맞는가** | 합성 데이터에서 잰 값이다(최대 24일). 실 ERP 는 다를 수 있고, 그러면 잘라내는 달 수가 바뀐다 |
| ~~비닐 단가~~ | ✅ **해소(2026-09-02)** — 1장 50원 · **박스 1개당 1장** → 개당 0.34원. BOM 에 반영됨 |
| **공정별 대기시간** | 체류일을 CT 비중으로 배분했으나 **줄 서서 기다린 시간은 모른다.** 실 ERP 의 공정 이동시각이 있어야 진짜 대기가 나온다 (현장 확인 *"공정 이동시간 기록됨"*) |
| 실험의 실제 실행 | `experiments`·`experiment_assignments` 는 **설계이지 실행 기록이 아니다** |

---

## 밖으로 나갈 때 (2026-09-05)

**이 폴더는 언젠가 배포된다.** 과제 제출물이 스트림릿 URL 이고, 그때 저장소가
통째로 남의 손에 넘어간다. **배포는 급할 때 하고, 급하면 안 훑는다** —
그래서 평소에 안 섞이게 두고 검사로 막는다. → `test_guards.py` 11절

| 안 쓴다 | 왜 |
|---|---|
| 볼트 노트명·폴더 경로 | 받는 사람에게는 **없는 위치**라 찾다가 만다 |
| 실명 거래처 | 제3자 이름은 우리가 낼 것이 아니다 |
| 내 PC 절대경로 | 남의 화면에서 아무 뜻이 없다 |

> ⚠️ **위 표에 예시 경로를 적지 않는다.** 2026-09-05 에 예시를 적었다가
> `test_guards` 11절이 이 문서를 잡았다 — 규칙을 설명하는 글이 규칙을 어긴 꼴이다.
> 검사 목록은 `tests/test_guards.py` 에 있고, **거기 한 곳에만** 둔다.

**대신 이렇게 쓴다** — *"2026-08 원재료 인상 대응 사내 실측(전수 2,561건 · 원문 비공개)"*
근거의 **성격과 규모**는 남긴다. 어디서 온 값인지 모르면 그 값을 못 믿는다.

⚠️ **회사명과 현장 발언 인용은 일부러 남겼다.** 수업 맥락에서 자연스럽고,
임계값 근거가 *"현장 기준 구술"* 이라는 사실이 지워지면 그 숫자의 출처가 사라진다.
지운 것은 **제3자 이름**과 **받는 사람이 못 여는 경로**뿐이다.

### 나갈 사본은 손으로 고르지 않는다 (2026-09-05 배포)

공개 저장소는 **볼트 밖 독립 저장소**다 — `Desktop/제작프로그램/production-flow-report`.
볼트 안에 두면 저장소가 겹치고, 비공개 볼트가 공개 원격에 딸려 갈 자리가 생긴다.

| | |
|---|---|
| 무엇을 빼나 | **`배포제외.txt` 한 곳** — `test_guards` 11절이 훑을 범위를 정할 때 읽는 그 파일 |
| 어떻게 옮기나 | `py -X utf8 _작업보조/배포사본-만들기.py --apply` (dry-run 기본) |
| 사본 쪽 `.git/` | **건드리지 않는다.** 지우면 이력이 통째로 날아간다 |
| 사본에만 있는 파일 | **지우지 않고 알리기만** 한다 — 손으로 고친 것이 있을 수 있다 |

★ **안 훑는 것은 안 내보낸다.** 배포일에 `_참고자료/` 안의 볼트 경로가 검사를
통과했는데, 잡아서가 아니라 **그 폴더를 훑지 않아서**였다. 목록이 둘이면
검사와 배포가 갈린다 — 그래서 파일 하나만 두고 둘 다 그것을 읽는다.

★ **개인 이름은 넣지 않는다** (2026-09-05, 23파일 71건 치환). 근거의 성격은 남긴다 —
*"현장 기준"* 이 지워지면 그 숫자를 왜 믿는지가 사라진다. → `test_guards` 11절이 검사한다.

| 어디에 | |
|---|---|
| 공개 저장소 | https://github.com/gabuki22/production-flow-report (2026-09-05 · 76파일 24MB) |
| 스트림릿 URL | (배포 후 여기 적는다) |

⚠️ **`gh` 는 PC마다 따로 로그인한다.** 이 PC는 안 돼 있어 배포일에 막혔다.
저장된 git 자격증명을 `gh` 에 넘겼더니 `read:org` 권한이 없다며 거절했는데,
**저장소 생성에는 필요 없는 권한**이라 `gh api -X POST user/repos` 로 직접 불러 통과했다.
Git Bash 는 `/user/repos` 의 앞 슬래시를 파일 경로로 바꾼다 → `MSYS_NO_PATHCONV=1` 또는 슬래시 제거.

---

## 이 앱이 하지 않는 것

- **지표를 스스로 정의하지 않는다.** 정의는 위키가 원본이고 앱은 읽어 쓸 뿐이다.
- **게이트를 대신 통과시키지 않는다.** 판단할 재료만 놓고 결정은 사람이 한다.
- **원본 데이터를 고치지 않는다.**
- **메일을 실제로 보내지 않는다.** 초안까지만 만든다.
