# 판단 기준

> 도메인이 바뀌어도 남는 문장만 적는다. 업종·데이터 이름이 들어가면 그건 예시다.
> 날짜별로 **덧붙인다. 덮어쓰지 않는다.**


---

## 4일치를 합치면 — 열둘 (2026-09-04 정리)

> 교안 프롬프트 13: *"화~금 4일치를 다 모아서 중복을 정리해줘."*
> 날짜별 기록은 아래에 **그대로 둔다.** 언제 어디서 데였는지가 그 문장을 지키게
> 만드는 힘이라, 합치면서 그것까지 지우면 규칙만 남고 이유가 사라진다.

### 데이터를 믿을 것인가 (화)

1. **차단과 경고를 가르는 것은 "계산이 틀리는가"다.** 틀리면 차단, 해석만
   조심하면 되면 경고. — *규칙을 늘리면 사람이 하나씩 끄기 시작한다.*
2. **메시지에는 숫자를 넣는다.** *"부족"* 은 판단 재료가 아니다.
   실제 값과 기준값이 같이 있어야 **사고인지 덜 쌓인 것인지** 갈린다.
3. **검증은 만든 것으로 끝나지 않는다. 깨뜨려 넣어 막히는지 본다.**
   → 목요일에 같은 방법으로 "일부러 걸어 보기"를 했고, 금요일에도 했다.

### 숫자를 낼 것인가 (수)

4. **무엇을 하나로 셀지는 데이터가 정해주지 않는다. 목적이 정한다.**
   분자와 분모의 **단위를 섞으면** 어느 쪽도 아닌 숫자가 된다.
5. **같은 이름의 값이 두 곳에서 다르면 대개 분모가 다르다.**
   작다고 뭉개지 말고 **왜 다른지 아는 상태**로 둔다.
6. **기준이 없으면 색을 칠하지 않는다.** 임계값 없는 카드는 값이 얼마든
   "정상"이라고 말하고 있는 것이다.
7. **이름이 안 맞는 개념을 남의 이름으로 부르지 않는다.** 대응이 정반대가 된다.

### 무엇을 감출 것인가 (목)

8. **못 믿으면 계산조차 하지 않는다.** 계산해 놓고 안 그리는 것과 다르다 —
   값이 변수에 들어 있으면 **리포트나 로그로 샌다.** *손에 없으면 못 쓴다.*
9. **감추되 사유는 자세히, 실제 숫자로.** 이유를 모르면 사람이 규칙을 우회한다.
10. **판정 순서를 코드로 박는다.** 좋은 결과를 먼저 보면 뒤 확인이 느슨해진다.
    그리고 **묻지 않은 물음을 적지 않으면 통과한 것으로 읽힌다.**

### 문장으로 내보낼 것인가 (금)

11. ★ **이 문장이 틀렸을 때 누가 책임지는가 — 그것으로 자동과 사람을 가른다.**
    **해석과 제안을 자동화하는 순간 책임의 주체가 사라진다.**
12. **검증이 잡은 것은 반드시 한계로 옮긴다.** 안 옮기면 없던 일이 된다.
    그리고 **찾았는데 없던 것도 적는다** — "없음"도 결과다.

### 나흘 내내 같은 말이었던 것

세 가지가 날마다 이름만 바꿔 나왔다. **합치지 않고 여기 한 번 더 적는다.**

| | 화 | 수 | 목 | 금 |
|---|---|---|---|---|
| **만든 것과 도는 것은 다르다** | 깨뜨려 넣어 본다 | 옛 전제를 든 검사가 FAIL 로 알려준다 | 검사기를 꺼 본다 | 일부러 걸어 본다 |
| **화면과 문서가 갈린다** | — | 같은 값이 두 화면에서 다르다 | 화면으로는 (가)/(나) 구분이 안 된다 | 화면에서 감췄는데 PDF 로 샌다 |
| **비어 있는 것이 안 보인다** | 경고만 띄우고 진행되는 검증 | 임계값 없는 카드가 늘 초록 | 근거 없이 통과되는 게이트 | 자동으로 채워진 빈 장 |

> **셋 다 "없는데 있는 것처럼 보이는" 문제다.** 그래서 확인법도 같다 —
> **없애 보고 정말 티가 나는지 본다.**

---

## 2026-09-01 — 검증을 만드는 쪽에서

### 배운 것

- **차단과 경고를 가르는 것은 "계산이 틀리는가"다.**
  틀린다 → 차단. 해석만 조심하면 된다 → 경고.

- **차단은 키가 없거나·중복이거나·순서가 역행하거나·값이 불가능할 때** 건다.
  **경고는 비어 있거나·적거나·치우쳐 있을 때** 건다.

- **검증 규칙은 적게 만든다.** 차단을 늘리면 데이터가 늘 어딘가 깨져 있어 앱이
  자꾸 멈추고, 사람이 차단을 하나씩 끄기 시작하고, 결국 검증 전체가 무력해진다.
  **지켜지는 만큼만 있는 것이 좋다.**

- **임계값에는 근거를 적는다.** 직전 기간 · 업계 기준 · 분석이 성립하는 최소치 ·
  조직의 합의 중 무엇이든 되지만, 적지 않으면 **근거 없는 임계값은 남의 것**이다.
  두 달 뒤 "왜 그 값이죠"에 답할 수 없으면 그 값은 내 것이 아니다.

- **메시지에는 숫자를 넣는다.** "부족"은 판단 재료가 아니다.
  실제 값과 기준값이 함께 있어야 **사고인지 덜 쌓인 것인지** 구분된다. 대응이 다르다.

- **결측은 오류일 수도 구조일 수도 있다.** 구조면 채우지 않는다. **채우면 위조다.**
  다만 조인에서 조용히 사라지므로 그 인원이 어디로 갔는지는 알고 있어야 한다.

- **이미 일어난 일과 앞으로 올 일을 같은 규칙으로 검사하지 않는다.**
  예정 값은 기간 밖이 정상이다. 관측처럼 검사하면 정상인 것이 전부 오류로 잡힌다.

- **검증은 만든 것으로 끝나지 않는다.** 깨뜨려 넣어 실제로 막히는지 본다.
  경고만 띄우고 진행되는 검증은 없는 것과 같다.

- ★ **에러로 죽는 것과 차단으로 멈추는 것은 다르다.** 죽으면 사람이 판단할 화면이 없다.
  그래서 **구조 검사는 계산보다 먼저 돈다.** 순서가 뒤집혀 있으면 검사기가 있어도 죽는다.

- ★ **판정이 상수로 박힌 검사는 검사가 아니라 문구다.** 화면에서는 통과한 것처럼
  보이지만 아무것도 보고 있지 않다. 초록불을 믿기 전에 **그 값이 계산된 것인지** 본다.

- **설정 한 곳만 고치면 되는 일은 없다.** 이름은 계산 코드 안에도 박혀 있다.
  바꾸고 켜면 깨지는 것이 정상이고, **한 번에 되면 오히려 뭔가 안 바꾼 것**이다.

### 오늘 내가 정한 것

| 규칙 | 판정 | 왜 |
|---|---|---|
| 행 수가 직전 적재의 50% 미만 | **차단** | 추출이 중간에 끊기면 분모가 조용히 줄고 **모든 비율이 틀린다** |
| 행 수가 직전 적재의 80% 미만 | 경고 | 줄었지만 계산은 성립한다. 사람이 볼 일 |
| 필수 컬럼이 없다 | **차단** | 키가 없으면 조인이, 날짜가 없으면 기간 집계가, 수량이 없으면 어떤 비율도 못 낸다 |
| 관측 값이 기간 밖 (5% 이하) | 경고 | 대개 추출 시점 차이다. 그 행만 빼거나 유효구간을 자르면 된다 |
| 관측 값이 기간 밖 (5% 초과) | **차단** | 그 정도면 기간을 잘못 잡은 것이고 분모가 실제로 틀어진다 |
| 예정 값이 기간 밖 | **규칙에서 제외** | 미래여야 정상이다. 검사하면 정상인 것이 전부 오류가 된다 |
| 참조 마스터에 없는 키가 있다 | **차단** | 그만큼 계산에서 조용히 빠진다 |

**적게 만든 이유**: 참고용으로 주어진 12건 중 3건만 만들었다. 나머지는 깨져도
계산에 영향이 없거나, 사람이 보고 넘길 수 있는 것이었다.

**규칙 값을 코드에 박지 않았다.** 임계·컬럼·기준은 설정 파일의 표 하나에 모으고
검사 코드는 그 표를 읽어 판정만 한다. 바뀌는 값이기 때문이다 —
바꿀 때마다 코드를 고쳐야 하면 결국 아무도 안 고친다.

### 오늘 걸린 곳

- 규칙을 다 만들고 깨뜨려 넣었더니 **1번 케이스에서 앱이 죽었다.** 차단이 아니라
  죽음이었다. 검사가 계산 뒤에 있어서, 컬럼이 빠지면 검사에 닿기 전에 계산이 터진 것.
  → **자가검증을 안 했으면 "검증 만들었다"고 넘어갔을 자리다.**
  규칙을 만든 뒤에는 반드시 **알려진 깨진 샘플로 검사기 자신을 검사한다.**

---

## 2026-09-02 — 숫자를 내는 쪽에서

### 배운 것

**어제와 오늘은 틀리는 방식이 다르다.** 어제는 규칙을 어기면 앱이 멈춰서 눈에 보였다.
오늘 만든 것은 **틀려도 숫자가 그냥 나온다.** 그래서 확인법이 바뀐다 —
깨뜨려 넣어 보는 것에서 **무엇이 정해졌는지 물어보는 것**으로.

**무엇을 하나로 셀지는 데이터가 정해주지 않는다.** 목적이 정한다.
"결국 됐는가"와 "이번에 됐는가"는 둘 다 맞는 숫자이고 다른 질문에 답할 뿐이다.

**분자와 분모의 단위를 섞으면 어느 쪽도 아닌 숫자가 된다.**
행으로 일관되게 세면 100%를 안 넘는다. 넘는 것은 분자만 행으로 바꿨을 때다.
100%를 넘는 순간 그레인이 틀린 것이 아니라 **단위를 섞은 것**이다.

**숫자가 안 맞으면 그레인 → 분모 → 필터 → 기간 순으로 의심한다.** 대개 첫 번째다.

**같은 이름의 값이 두 화면에서 다르게 나오면 대개 분모가 다르다.**
작다고 뭉개지 말고 **왜 다른지 아는 상태**로 둔다. 모르고 같다고 하면 언젠가 인용된다.

**앞 단계를 안 거치고 온 대상이 많으면 퍼널이 아니라 분류다.**
분류에 전환율을 붙이면 안 된다 — 분모가 앞 단계가 아니기 때문이다.

**전환율이 100%인 단계는 아무것도 거르지 않는다.** 단계로 둘 이유가 없다.
넣어 보고 100%가 나오면 그 단계는 빼는 것이 맞다.

**관측 기간이 다른 것을 누적값으로 비교하지 않는다.**
가장 최근 것이 낮은 건 성과가 아니라 시간의 문제일 수 있다.

**임계값이 없는 지표는 판정하지 않는 것이 아니라 "정상"이라고 말하고 있는 것이다.**
비워 두면 값이 얼마든 초록으로 뜬다. 채우기 전까지 그 카드는 장식이다.

**기준이 없으면 색을 칠하지 않는다.** 좋고 나쁨을 가를 선이 없는데 초록을 칠하면
"정상"이라고 단언한 셈이다. 판정 색은 판정에만 쓴다.

**이름이 안 맞는 개념을 남의 이름으로 부르지 않는다.**
대상이 사라지는 도메인과 사라지지 않고 급해지는 도메인은 대응이 정반대다 —
하나는 되찾는 일이고 하나는 먼저 처리하는 일이다. 같은 말로 부르면 대응을 틀린다.

**단가만으로는 개당 원가가 안 나온다.** 하나가 몇 개를 담는지가 300배를 가른다.
값을 받았을 때 **그 값이 무엇 하나당인지**를 같이 묻지 않으면 내가 정하게 된다.

### 오늘 내가 정한 것

- **그레인 = 대상 1건.** 한 대상이 같은 단계를 두 번 밟을 수 있어서(되돌아오는 구조가
  있다) 행이 아니라 고유값으로 센다. 행으로 세면 되돌아온 것을 다시 세게 된다.
- **분모 = 판정 시점이 지난 것 전체.** 완료된 것만 세면 어려운 건을 미룰수록
  지표가 올라간다.
- **관측 커트라인을 둔다.** 판정이 가능해지기까지 걸리는 기간만큼 잘라낸다.
  안 자르면 진행 중인 것이 실패로 들어가 최종 통과율이 9%p 넘게 나쁘게 나온다.
- **유지 단계는 후보를 실측으로 늘어놓고 골랐다.** 12개를 건수와 함께 보고
  그중 셋을 순서대로 놓았다. 4단계로 넣었더니 마지막이 100%라 하나를 뺐다.
- **되돌아온 적이 있는가**를 유지 단계에 끼웠다. 이 축은 획득 퍼널에서는 뺀 것인데,
  유지 축에서는 "한 번에 통과했나"를 묻는 신호가 된다.
- **유예 기간을 0으로 두었다.** 처음엔 30일을 넣었는데, 현장은 기한 다음 날부터
  급해진다. 유예는 내가 넣은 것이지 현장에서 온 값이 아니었다.
- **임계값은 한 줄로 두었다.** 숫자를 하나만 받았으므로 경고와 위험을 같은 값으로
  두고 "넘으면 바로 위험"으로 했다. **없는 숫자를 만들지 않았다.**

### ★ 도구가 말 안 하고 정한 것 중 잡아낸 것

물어보지 않으면 안 나온다. 오늘 물어서 나온 것들이다.

| 무엇 | 어떻게 드러났나 |
|---|---|
| **주석이 사실과 달랐다** | "행으로 세면 100%를 넘는다"고 적혀 있었는데, 실측하니 행/행으로는 안 넘었다. 넘는 것은 단위를 섞을 때였다 |
| **첫 도달/마지막 도달을 내가 골랐다** | 우리 데이터에서는 값이 같아 티가 안 났다. 중복이 날짜까지 같아서였다 — **실데이터로 바꾸면 갈린다** |
| **유지 단계를 후보 없이 정했다** | 교안이 "후보만 나열하고 순서는 사람이 정한다"고 한 자리인데, 물어본 적 없이 내가 정해 놓고 있었다 |
| **유예 기간 30일을 내가 넣었다** | 현장 기준은 0이었다. 숫자 하나가 정의를 바꿨다 |
| **임계값 없는 카드가 둘 있었다** | 값과 무관하게 늘 초록이었다. 100%가 돼도 정상으로 떴을 것이다 |
| **판정 색을 코드에 박아 놨다** | 값을 안 보고 늘 같은 색이었다. 판정이 아니라 장식이었다 |
| **"두 값이 같다"고 단언했다** | 분모가 달라 0.06%p 차이가 났다. 검사가 잡았다 |
| **폴더를 복제하며 숨김 파일을 빠뜨렸다** | 이름을 나열해 복사했더니 점으로 시작하는 것이 조용히 빠졌다. 통째로 복사한 뒤 뺄 것을 지우는 편이 안전하다 |

### 오늘 걸린 곳

- **검사가 내 가정을 두 번 깼다.** 한 번은 "분모가 넓어질수록 값이 낮아진다"(결측
  처리가 분모 선택보다 크게 움직였다), 한 번은 "두 값이 같다"(분모가 달랐다).
  **둘 다 손으로는 못 봤을 것이다.**
- **검사가 옛 전제를 들고 있으면 FAIL 로 알려준다.** 임계값을 넣자
  "임계값이 없다"를 전제한 검사가 걸렸다. 지우지 말고 **새 사실로 바꿔 쓴다.**
- **단계를 늘렸더니 검사가 옛 이름으로 죽었다.** 목록을 검사에 박지 말고
  설정에서 가져와야 한다. 화면 목록도 마찬가지다 —
  카드를 다섯으로 늘렸을 때 칸이 넷이라 **다섯 번째가 조용히 사라졌다.**

---

## 2026-09-04 (Day3 진도) — 무엇을 보여주고 무엇을 감출지 정하는 쪽에서

### 배운 것

- ★ **"이 축으로는 안 갈린다"는 축의 문제가 아니라 보는 값의 문제일 수 있다.**
  같은 축·같은 데이터인데 무엇을 쪼개느냐로 격차가 0.6%p 와 4.2%p 로 갈렸다.
  **병목이 "새는 것"이 아니라 "밀리는 것"이면 탈락률에는 아무것도 안 나타난다.**
  축을 늘리기 전에 **지금 쪼개고 있는 값이 우리 문제와 같은 축인지** 먼저 본다.

- ★ **모든 지표가 같은 날 확정되는 것은 아니다.**
  어떤 지표는 기준일이 지나는 순간 판정이 끝나고, 어떤 지표는 그 뒤로도 며칠에
  걸쳐 값이 바뀐다. **후자는 데이터 끝에 가까운 구간이 무조건 좋아 보인다** —
  나쁜 결과가 아직 도착하지 않았을 뿐이다.
  **확정에 걸리는 시간을 실측해서 그만큼 잘라낸다.** 추측으로 정하지 않는다.

- **한 화면에서 지표마다 비교 구간이 다를 수 있다.** 그러면 한 줄로 뭉뚱그리지
  말고 **각자의 구간을 각자 옆에 적는다.** 안 적으면 다른 구간끼리 비교한 것을
  같은 구간으로 읽는다.

- ★ **결과만 보이고 과정이 안 보이면, 사람은 마지막 배지 하나로 전부를 판단한다.**
  특히 **묻지 않은 물음**을 적지 않으면 통과한 것으로 읽힌다.
  "가드레일 이상 없음"과 "가드레일을 보지도 않았음"은 화면에서 구분되지 않는다.
  **판정을 낼 때 그 판정이 어디서 갈렸는지도 같이 남긴다.**

- ★ **게이트에 근거를 안 적어도 통과되면, 그 기록은 기록이 아니라 도장이다.**
  통과 시각과 통과자는 자동으로 남지만 **무엇을 보고 통과시켰는지는 사람만 쓸 수
  있다.** 게이트는 앱이 판단하지 않는 자리이므로 그 문장이 그 자리의 유일한
  산출물이다. **비워 둘 수 있게 만들면 반드시 비어 있다.**

- **여러 판단의 근거를 한 줄로 이어 붙이지 않는다.** 묻는 것이 다른 두 판단이
  한 문장이 되면, 읽는 사람은 그것을 한 번의 판단으로 읽는다.

- **카드에 사유를 다 적으면 아무도 안 읽고, 요약만 두면 우회한다.**
  한 문장은 카드에, 전부는 눌러서 여는 자리에. **이유를 모르면 사람은 규칙을
  우회하려 한다** — 감추는 일과 설명하는 일은 같이 가야 한다.

- **정의를 화면에 펼쳐 두면 매일 보는 사람에게는 소음이고, 없으면 분모를 모르는
  사람이 같은 숫자를 다르게 읽는다.** 접어서 옆에 둔다.

- **URL 은 사람이 고칠 수 있는 입력이다.** 링크로 상태를 주고받게 만드는 순간
  그 값은 남이 쓴 값이 된다. **허용 목록으로 거르지 않으면 낡은 링크 하나에
  화면이 죽는다.**

### 오늘 걸린 곳

| 걸린 것 | 무엇이 문제였나 |
|---|---|
| **끝 달만 지표 둘이 반대로 튀었다** | 하나는 과대, 하나는 과소. 개선이 아니라 **관찰 기간이 모자란 것**이었다. 둘이 반대로 움직이면 경계 효과를 의심한다 |
| **판정 라벨만 조용히 비었다** | `numpy.bool_(False) is False` 가 False 라 분기가 안 걸렸다. **값도 판정도 맞고 라벨만 틀려서** 눈으로는 못 찾는다 |
| **카드는 4개인데 추이는 2개였다** | 화면에 안 보이는 지표는 "괜찮은 것"으로 읽힌다. 세워 놓고 변화를 안 보여주면 나빠지는 중이어도 아무도 모른다 |
| **없는 단계 이름으로 격차가 0.00%p 로 나왔다** | 내가 만든 값이었다. **정확히 0 은 대개 버그 신호다** — 진짜 0인지 확인하고 넘어간다 |
| **저장된 게이트 기록이 둘 다 비어 있었다** | 입력칸은 있었지만 비워도 눌리는 버튼이었다. **화면에만 있는 규칙은 계산 검사로는 안 잡힌다** |

### 이번에 쓴 방법

- **검사기를 만들면 그 검사기를 꺼 본다.** 규칙을 빼면 실제로 FAIL 하는지
  확인하고 되돌린다. 검사기와 대상이 같은 전제를 공유하면 **둘이 같이 망가지고
  둘 다 조용히 통과한다.**
- **잘못된 입력을 직접 넣어 본다.** 없는 축 이름과 범위 밖 날짜를 URL 에 넣어
  예외 없이 기본값으로 돌아오는 것을 눈으로 봤다. 방어는 짜 놓은 것이 아니라
  **깨뜨려 본 것**만 방어다.

---

## 2026-09-04 (Day4 진도) — 숫자를 문장으로 바꾸는 쪽에서

### 배운 것

- ★★ **이 문장이 틀렸을 때 누가 책임지는가 — 그것으로 자동과 사람을 가른다.**
  사람이 책임지면 사람이 쓴다. 사실이 틀린 것뿐이면 자동으로 쓴다.
  **해석과 제안을 자동화하는 순간 책임의 주체가 사라진다.** 나흘의 결론이다.

- **자동으로 쓴 문장이 더 위험하다.** 사람이 쓰면 틀렸을 때 그 사람에게 묻지만,
  자동은 **물을 사람이 없고 읽는 사람은 사실로 읽는다.** 같은 문장인데 무게가 다르다.

- **비어 있으면 비어 있는 대로 찍는다.** 없는 것을 자동으로 채우면 **아무도 빈 줄
  모른다.** 빈칸이 보이는 것이 그 칸의 일이다.

- ★ **코드가 자기 문장을 검사한다. 그리고 사람이 쓴 곳에도 건다.**
  자동 문장은 조심해서 만들지만 사람이 쓴 해석에는 인과 표현이 훨씬 자주 들어간다.
  **검사가 가장 필요한 곳에 안 걸려 있기 쉽다** — 만들었다고 다 도는 것이 아니다.

- **금지어를 늘릴 때는 말마다 근거를 적는다.** 근거 없이 늘어난 목록은 나중에
  누가 통째로 지운다. 판단 기준은 하나 — **이 말이 상관을 인과로 바꾸는가.**

- ★ **한계는 세 곳에서 온다. 그중 "찾았는데 없던 것"이 늘 빠진다.**
  검증이 잡은 것 · 못 한 것 · **확인했는데 신호가 없던 것**.
  *"없었다"도 결과*라 안 적으면 다음 사람이 같은 것을 또 찾는다.
  **경고가 났는데 한계에 없으면 그 경고는 사라진 것과 같다.**

- **한계 절에는 조건부 항목과 무조건 항목이 섞여 있다.**
  데이터가 좋아지면 사라지는 항목(경고·표본 부족)과, **분석의 성격이라 어떤
  데이터에서도 사라지면 안 되는 항목**(관측이라 인과 불가 · 기간의 한계 ·
  데이터의 출처)이 다르다. 뒤엣것은 조건부 항목 사이에 두면 언젠가 조용히 없어진다.

- **한계를 적으면 신뢰가 떨어지지 않는다.** 안 적으면 틀린 게 하나 나왔을 때
  **전부를 의심받는다.** 적으면 그 부분만 재검토된다.
  다만 *"여러 한계가 있어 해석에 주의가 필요함"* 은 아무 정보가 없다 —
  **무엇이 한계이고 그래서 무엇을 못 하는지**를 적어야 한다.

- ★ **되돌릴 수 없는 자리는 그 앞에서 걸러야 한다.**
  그리고 **사람이 매번 기억해서 하는 점검은 언젠가 건너뛴다** — 코드로 박고,
  걸리면 통과 자체를 잠근다. 화면이 그 무게를 보여줘야 한다.

- **감췄으면 문서에서도 감춰야 한다.** 한쪽만 감추면 감춘 의미가 없다.
  다만 **조건 값**(표본 수·기간·배정 비율)은 보여주는 쪽이다 —
  감출 것은 **지표 값**(효과·증감·p값·신뢰구간)이다. 이 둘을 섞으면
  "왜 감췄는지"까지 사라져서 사람이 규칙을 우회한다.

- **기다리게 할 거면 무슨 일이 일어나는지 보여준다.** 멈춘 화면을 보면
  사람은 버튼을 또 누른다. 그리고 **실패를 조용히 삼키지 않는다** —
  됐다고 표시됐는데 안 된 것이 가장 나쁘다.

- ★ **"자연스러운가? 이 화면이 맞나"를 스스로 물어야 한다** (2026-09-04 강사 코멘트).
  도구가 내준 화면은 **돌아가는 것**이지 **맞는 것**이 아니다. 돌아가면 넘어가기 쉬운데,
  넘어가면 그 화면이 기본값이 된다. 만들고 나서 한 번은 **읽는 사람 자리에서** 본다.

  > 오늘 이것을 안 물어서 넷을 놓칠 뻔했다 — 사람 장 검사·항상 문장·요약의 변화·
  > 결과의 분해. 전부 화면은 멀쩡했고 **없는 것이 안 보였을 뿐**이다.

- **개발 전공이 아니면 "어떤 파일이 무엇을 하는가"를 따로 공부해야 한다**
  (2026-09-04 강사 코멘트). 도구에 시키기만 하면 고칠 곳을 못 찾는다.
  ★ 그래서 이 앱은 **파일마다 맨 위에 그 파일이 하는 일**을 적어 두고,
  `CLAUDE.md` 에 파일 표를 둔다 — 전공자가 아니어도 **어디를 열지**는 알 수 있게.

### 오늘 내가 정한 것

- **자동 5장 / 사람 3장.** 요약·방법·결과·실험·한계는 자동, 배경·해석·제안은 사람.
  가른 기준은 위의 첫 문장 하나다.
- **금지어 12개.** 기본 여섯에 내 분야 표현 여섯을 더했고 **말마다 근거를 달았다.**
- **한계에 무조건 들어가는 문장 셋** — 관측이라 인과 불가 · 기간의 한계 ·
  데이터의 출처. 조건부 항목보다 **앞에** 둔다.
- **발송 전 점검 5항목**을 코드로 박고, 하나라도 걸리면 게이트를 잠근다.
- **되돌릴 수 없는 게이트에도 통과 근거를 요구한다.** 가장 되돌릴 수 없는 자리인데
  근거가 자동 문구로 박혀 있었다.

### 오늘 걸린 곳

| 걸린 것 | 무엇이 문제였나 |
|---|---|
| **검사가 가장 필요한 곳에 안 걸려 있었다** | 자동 장에서만 돌고 **사람이 쓴 장은 아예 검사되지 않았다.** 만들었다고 다 도는 것이 아니다 |
| **"항상 넣는다"고 정한 문장이 빠져 있었다** | 조건부 항목만 조립하고 무조건 항목을 안 넣었다. 없어도 문서는 그럴듯하게 나온다 |
| **문서가 코드보다 낡아 있었다** | 규칙을 바꿨는데 그 규칙을 설명하는 장은 옛 규칙을 적고 있었다. **문서는 조용히 낡는다** |
| **유출을 찾다가 헛짚었다** | 문서 전체에서 숫자를 찾으니 *감추지 않은* 항목의 값이 걸렸다. **감춘 것 주변만** 봐야 한다 |
| **헤더 파싱이 별표를 못 벗겼다** | 뒤에 괄호가 붙어 있어 끝 별표가 안 잘렸고, 한 무리가 통째로 다른 출처로 들어갔다 |
| **위젯을 바꾸니 검사가 엉뚱한 칸에 썼다** | 위치로 집고 있었다. **위치가 아니라 이름으로 집는다** |

### 이번에 쓴 방법

- **일부러 걸어 본다.** 검사를 만든 뒤 규칙을 어기는 입력을 직접 넣고,
  ①경고가 뜨는가 ②**무엇이 걸렸는지 보이는가** ③되돌리면 사라지는가를 눈으로 봤다.
  ②가 없으면 사람이 고칠 수 없어 검사가 있으나 마나다.
- **결과물을 뽑아서 검색한다.** 화면만 보지 않고 실제 산출물(PDF·메일 초안)의
  텍스트를 뽑아 감춰야 할 값이 있는지 찾았다. **매체가 다르면 규칙도 따로 샌다.**
- **점검을 사람 기억에서 코드로 옮긴다.** 한 번 확인한 것은 검사로 남긴다.
  같은 것을 다음에도 사람이 기억해야 한다면 그것은 아직 안 끝난 것이다.

---

## 2026-09-05~06 (배포) — 밖으로 내보내는 쪽에서

교안 진도가 아니라 **만든 것을 남의 손에 넘기는 날**이었다.
넘기는 순간 규칙의 성격이 바뀐다 — 안에서는 지키면 되는 것이, 밖에서는 **막아야 하는 것**이 된다.

### 배운 것

- **검사가 안 훑는 곳의 통과는 통과가 아니라 무지다.** 초록불이 켜졌지만
  잡아서가 아니라 보지 않아서였다. 제외 목록과 검사 범위는 **같은 파일에서** 읽는다.
- **차단은 행위의 *형태*에 걸린 것이지 *목적*에 걸린 것이 아닐 수 있다.**
  막히면 둘을 가르고, 형태였으면 노출이 더 적은 형태를 찾는다.
- **도구가 자기 기준으로 거절하는 것**과 권한이 실제로 없는 것은 다르다.
  편의 도구는 자기가 쓰는 모든 기능의 권한을 한꺼번에 요구한다.
- **같은 설정이 기계마다 따로** 있는 것이 있다. "저번엔 됐는데"는 코드가 아니라
  그 기계의 상태를 묻는 말이다.
- **규칙을 설명하는 글이 그 규칙을 어기기 쉽다.** 금지 목록은 검사기 한 곳에만 둔다.
- **한 화면에 여러 칸을 쌓으면 아래 칸이 빈다.** 하나씩 열면 그 칸을 쓴다.

### 오늘 내가 정한 것

| 정한 것 | 왜 |
|---|---|
| **공개용은 별도 저장소로 내고, 원본은 비공개 그대로 둔다** | 원본 안에 두면 저장소가 겹쳐, 안 나가야 할 것이 공개 원격에 딸려 갈 자리가 생긴다 |
| **설명하려고 사람 이름을 넣지 않는다** | 근거로 필요한 것은 *"현장에서 정한 값"* 이라는 성격이지 누구인지가 아니다. **성격은 남기고 이름만 뺐다** |
| **회사 이름과 구술 인용은 일부러 남긴다** | 임계값의 근거가 구술이라는 사실까지 지우면, 읽는 사람이 그 숫자를 왜 믿어야 하는지 알 수 없다. 지운 것은 제3자 이름과 **받는 사람이 못 여는 경로**뿐 |
| **통과 버튼을 바로 누를 수 있게 한다** | 근거를 매번 타이핑하는 것이 통과를 막는 벽이 됐다. ⚠️ 단, **칸은 없애지 않았다** — 없애면 기록이 다시 빈다. 타이핑을 **고르기**로 바꿨다 |
| **사람이 쓰는 칸은 번호를 눌러 하나씩** | 셋을 한 화면에 쌓으니 어느 칸을 쓰는 중인지 눈이 흩어지고, 뒤쪽이 빈 채로 남았다 |
| **교안이 명시한 형태는 내 취향으로 바꾸지 않는다** | 사람 3란을 나누되 *"교안에 나왔다면 그대로 둔다"* 를 조건으로 걸었다 |
| **골격에서 온 제목을 우리 것으로 갈았다** | 단계도 지표도 전부 바꿔 놓고 **제목만 남의 것**이었다. 빌려온 이름이 남으면 빌려온 사고도 남는다 |
| **분해 축을 체류일수·구간 분해로 정하고, 낡은 수치 4건은 고친다** | 통과율로는 축 간 격차가 0.6%p 뿐이라 아무것도 안 갈렸다. **볼 지표가 안 맞았던 것** |

### 오늘 걸린 곳

| 걸린 것 | 무엇이 문제였나 |
|---|---|
| **검사에서 뺀 폴더 안에 걸릴 것이 있었다** | 통과했지만 본 적이 없었다. 배제 목록과 검사 범위가 **다른 곳에** 있었다 |
| **막힌 형태 하나를 목적 전체로 읽었다** | "이 길은 닫혔다"고 결론 내려 상대에게 세 번 되돌려 보냈다. 형태를 바꾸니 한 번에 됐다 |
| **본문의 수치가 재생성을 안 따라왔다** | 낡음을 잡는 장치를 만들었는데 **그 문서가 사정권 밖**이었다 |
| **밖으로 내는 커밋에 개인 메일이 박혔다** | 이름을 빼면서 메일을 공개하면 앞뒤가 안 맞는다. 푸시 **전에** 잡았다 |

### 이번에 쓴 방법

- **바꾸기 전에 무엇이 바뀌는지 전부 보여준다.** 71건 치환을 미리보기로 먼저 냈고,
  거기서 오타 하나를 잡았다.
- **원본은 열지 않고 사본을 연다.** 검증을 통과한 것만 원본 자리로 올린다.
- **훑지 않은 것은 내보내지 않는다.** 목록을 하나로 두고 검사와 배포가 같이 읽는다.
- **고치는 중에는 검사를 돌리지 않는다.** 확인은 미덕이지만 매 수정마다는 지연이다.

## 2026-09-08 (Day1 진도) — 숫자를 근거로 바꾸는 쪽에서

지금까지는 **숫자가 맞는가**를 물었다. 오늘부터는 **이 숫자로 남을 움직일 수 있는가**를 묻는다.
둘은 다르다. 맞는 숫자인데도 근거가 안 되는 것이 있다.

### 규칙

- **맞는 숫자와 근거가 되는 숫자는 다르다.** 정확해도 분모가 없으면 아무도 결정하지 못한다.
- **근거에는 넷이 있어야 한다** — 분자·분모 · 비교 대상 · 비중 · 표본.
  비중을 모르면 그게 큰일인지 모른다. 값 하나로는 크기를 말할 수 없다.
- **근거는 조회한 것이다.** 요약에서 옮겨 적은 것이 아니다.
  요약을 요약하면 틀린 곳을 찾을 수 없게 된다.
- **같은 비율이라도 분모가 무엇이냐에 따라 다른 값이다.** 어느 분모인지 적는다.
  ★ 한 화면 안에서도 갈린다 — 카드와 분해가 같은 이름의 지표를 다른 모집단으로 낼 수 있다.
- **한 건이 바뀌어 값이 격차보다 크게 흔들리면 그 축으로는 결론을 못 낸다.**
  값이 틀린 것이 아니라 격차가 없는 것이다.
- **작은 수를 비율로 쓰면 큰 표본처럼 읽힌다.** 분모가 작으면 비율 말고 건수로 쓴다.
- **최근 구간이 낮은 것은 대개 악화가 아니라 시간이 안 찬 것이다.**
- **화면에서 감춘 값을 문서에 쓰면 감춘 것이 없던 일이 된다.**
- **발견은 숫자가 아니라 문장이다.** 값 · 비교 · 비중 세 줄이 있어야 한다.
- **발견에 원인을 섞지 않는다.** 섞으면 나중에 그 문장을 정당화하게 된다.
- **"갈리지 않는다"도 결과다.** 안 적으면 다음 사람이 같은 축을 또 쪼갠다.

### 오늘 내린 결정

| 결정 | 무엇을 · 왜 |
|---|---|
| **크기를 격차 × 비중으로 쟀다** | 격차만 보면 가장 작은 칸이 1위로 올라온다. 격차 최대 칸과 크기 최대 칸이 **2.8배** 벌어졌다 |
| **격차가 큰 칸을 1순위로 삼지 않았다** | 그 칸의 비중이 3%대라 다 고쳐도 전체가 거의 안 움직인다. 비중 두 자리 칸을 앞에 뒀다 |
| **퍼널 전환율로 낸 축 분해를 버렸다** | 세 축 전부 격차 0.3%p 미만. 표본 탓이 아니다(최소 분모 2,502) — **볼 지표가 안 맞았다** |
| **감춘 실험의 이름이 겹치는 발견을 남겼다** | 감춘 것은 *그 실험의 효과*이고 발견은 *관측된 격차*다. 다른 것이라 남기되, **그 실험이 하려던 주장은 쓰지 않는다** |
| **표본 검사를 만들되 안 걸릴 것을 알고 만들었다** | 우리 분모로는 한 번도 안 걸린다. 그래도 만든다 — 더 잘게 쪼개면 바로 걸리고, **그때는 안 만든 걸 모른다** |

### 오늘 걸린 곳

| 걸린 것 | 무엇이 문제였나 |
|---|---|
| **구조를 바꾸니 그 구조로 잰 숫자가 전부 낡았다** | 퍼널을 6공정으로 쪼개 병목 구간 이름이 바뀌었는데, 그 구간으로 잰 격차가 문서 세 곳에 옛 값으로 남아 있었다 |
| **같은 지표에 분모가 둘이었다** | 카드와 축 분해가 이름은 같은데 모집단이 달랐다. 나란히 인용하면 다른 두 수를 같은 것처럼 쓰게 된다 |
| **문서가 값을 안 싣고 있었다** | 검사가 *"문서에서 이 값을 글자로 찾는다"*고 되어 있는데 한쪽 문서에 아예 값이 없었다 |
| **최저 칸만 보고 크기를 낼 뻔했다** | 축마다 최저 칸 하나씩만 재면 **비중 큰 중간 칸이 안 보인다.** 칸마다 다 재야 한다 |

### 이번에 쓴 방법

- **화면을 읽지 않고 함수를 부른다.** 화면은 반올림돼 있고 분모가 안 보인다.
- **안 걸리는 검사는 일부러 걸어서 확인한다.** 실데이터로만 시험하면
  규칙이 죽어 있어도 전부 통과한다. 깨진 표본을 넣고, **규칙을 꺼서 미검출까지** 본다.
- **"없었다"를 적어 둔다.** 찾았는데 없던 것은 다음 사람에게는 안 찾아본 것과 구분되지 않는다.

## 2026-09-09 (Day2 진도) — 문서를 화면 안에서 만드는 쪽에서

어제까지는 근거가 갖춰진 문장을 **파일로** 만들었다. 오늘은 그 문장을
**화면에서 바로 문서로 조립하고 받아 가게** 했다. 명령을 돌리는 것과
기능을 만드는 것은 다르다.

### 배운 것

- **문서는 자동으로 쓰는 절과 사람이 쓰는 절로 나뉜다.**
  판단이 들어가는 절을 자동화하면 **책임의 주체가 사라진다.**
- **절을 나누는 기준은 하나다 — 다시 조회하면 똑같이 나오는가.**
  그렇다면 자동, 아니면 사람이다. 애매한 절이 하나 나오는데,
  거기서는 **후보를 자동으로 나열하고 그중 무엇을 고를지는 사람이 정한다.**
- **없는 것을 화면에 채우지 않는다.** 없으면 "아직 안 정함"으로 정직하게 남긴다.
  채워서 보여주면 그럴듯해 보이지만 **안 정한 것을 정한 것처럼** 만든다.
- **비어 있다는 사실도 내용이다.** 빈 목록을 조용히 보여주면 *아무것도 안 정한
  것처럼* 읽힌다 — 왜 비었는지를 그 자리에 적는다.
- **본문 순서를 바꾸지 않는다.** 하자는 것을 앞에 두면 읽는 사람이 거기서 멈추고,
  **멈추자는 말은 영영 안 읽힌다.**
- **검사는 화면에서도 걸어야 한다.** 화면에서 지키고 문서에서 안 지키면 의미가 없다.
- **기능을 먼저 만들고 자리는 나중에 정한다.** 자리를 먼저 정하면 아직 없는
  메뉴에 맞춰 코드를 짜게 되고, 시간의 절반을 배선에 쓴다.
- ★ **없는 것을 전제로 만든 것은, 그것이 도착하는 순간 버린다.**
  둘을 남겨 두면 어느 것이 진짜인지 알 수 없게 된다.
- ★ **형식의 정본이 파일로 있으면 코드에 옮겨 적지 않고 읽는다.**
  옮겨 적으면 원본이 바뀌었을 때 **우리 것만 낡는다.**
- ★ **도구가 못 그리는 글자는 어느 매체에서도 쓰지 않는다.**
  매체마다 글자를 바꾸면 **한 본문이 매체마다 달라져 어느 쪽이 원본인지 알 수 없다.**

### 오늘 내가 정한 것

**어느 절을 자동으로, 어느 절을 사람으로 나눴는가**

| 절 | 누가 쓰나 | 왜 그렇게 정했나 |
|---|---|---|
| 한 장 요약 | **자동** | 재료에서 조립한 것이라 다시 조회하면 같은 값이 나온다 |
| 멈출 것 · 다시 잴 것 · 할 것 | **자동** | 카드에 적힌 것을 **옮기는** 일이지 새로 짓는 일이 아니다 |
| 이것이 틀린다면 | **사람** | 무엇 때문에 틀릴지는 데이터가 아니라 판단이다 |
| 적용 | **사람** (후보만 자동) | 후보는 기록에서 나오니 자동, **무엇을 고를지는 사람** |
| 부록 | **자동** | 근거를 다시 모으는 일이다 |

| 그 밖에 정한 것 | 왜 |
|---|---|
| **자동 절에는 입력창을 만들지 않는다** | 막는 것이 아니라 **없다.** 있는데 막으면 언젠가 풀린다 |
| **화면은 재료 파일을 읽기만 한다** | 화면에서 값을 고치기 시작하면 재료와 화면 중 어느 것이 진짜인지 알 수 없게 된다 |
| **사람이 쓴 글은 문서마다 파일을 나눠 남긴다** | 한 파일에 섞으면 이름이 겹치는 날 한쪽이 덮이는데, **덮인 쪽은 되살릴 방법이 없다** |
| **안 쓰는 절은 빈칸으로 두지 않고 통째로 뺀다** | 빈칸으로 두면 **못 한 것이 안 보인다** |
| **검사는 고치는 중에 안 돌리고 마지막에 한 번** | 확인은 미덕이지만 매 수정마다는 지연이다 |
| **남에게 받은 것은 우리가 다시 내지 않는다** | 제외 목록에 넣어 공개본에서 뺐다. 안 넣으면 그대로 나간다 |

### 오늘 걸린 곳

| 걸린 것 | 무엇이 문제였나 |
|---|---|
| **산문에서 값을 긁었더니 짝이 어긋났다** | 한 문장에 여러 칸의 숫자가 섞여 있어(A는 얼마 vs B는 얼마) 서로 다른 칸의 값이 한 줄로 뽑혔다. **표에 칸을 만들어 그 칸만 읽게** 고쳤다 |
| **표에 칸을 늘렸는데 읽는 쪽 목록을 안 늘렸다** | 화면은 *"값이 없다"* 고 **정직하게** 말했는데 — 원본에는 있었다. **조용히 못 읽은 것**이라 눈으로는 못 찾는다 |
| **도구가 못 그리는 글자가 조용히 네모로 나갈 뻔했다** | 경고가 화면이 아니라 로그로만 나가서, 결과물을 열어 봐야 알고 열어도 못 알아본다 |
| **화면의 값을 직접 고쳤더니 저장은 됐는데 내용이 비었다** | 보이는 것만 바꾸고 프로그램까지는 안 갔다. **보이는 것을 고치는 것과 입력하는 것은 다르다** |
| **없는 것을 전제로 형식을 만들었다** | 원본이 도착해서 버렸다. 처음부터 *"없으면 어떻게 할 것인가"* 를 정해 뒀으면 버릴 것이 적었다 |

### 이번에 쓴 방법

- **옮길 때는 들여쓰기만 하고, 공백 무시 비교가 비는 것으로 "그대로"를 증명한다.**
  손으로 옮기면 뭔가 빠지는데, 빠진 것은 나중에 찾기 어렵다.
- **규칙을 일부러 어겨 검사가 실제로 잡는지 보고 되돌린다.**
  통과만 보고 넘어가면 검사가 도는지 모른다.
- **기본값 있는 인자를 더해 기능을 늘린다.** 기존 호출을 한 줄도 안 고치고 늘어난다.
- **없는 것을 세어서 알리되 채우지 않는다.** 몇 자리가 비었는지가 정보다.


---

## 2026-09-10 (Day3 진도) — 문서를 결정 요구로 끝내는 쪽에서

어제까지는 **맞는 문장**을 만들었다. 오늘은 그 문장이 **결정을 받아내는가**를 봤다.
읽는 사람이 바뀌면 같은 내용이라도 들어갈 것과 뺄 것이 갈린다.

### 배운 것

- **제안서는 분석 문서가 아니라 결재 문서다.** 읽는 사람이 결정 권한을 가진
  사람이라 **계산 과정이 들어가지 않는다** — 궁금하면 도구를 열면 된다.
- **문서의 마지막 줄이 결정을 요구하는 문장이 아니면** 읽은 사람은 무엇을
  해야 하는지 모른 채 문서를 닫는다.
- **채울 함수를 못 대는 절은 만들지 않는다.** 자리를 비워 두는 것과 자리를
  안 만드는 것은 다르다 — 비워 두면 그 자리가 **그대로 인쇄된다.**
- **모르는 것을 낱말 하나로 두면 아무도 자기 일인 줄 모른다.**
  [무엇을 모르는가] — [누가 확인하는가] — [모르는 채로 할 수 있는 결정],
  셋이 갖춰져야 구멍이 아니라 **요청**이 된다.
- ★ **갈래가 내 데이터에서 안 나오면 갈래를 바꾼다.** 없는 차이를 키우는 것이
  아니라 **같은 대상을 다른 값으로 다시 보는 것**이다. 안 나온 갈래는
  지우지 말고 **기각으로 남긴다** — 안 본 것과 보고 아니었던 것은 다르다.

### 오늘 내가 정한 것

**절 여섯 — 읽는 사람이 결정하는 순서대로 놓았다. 순서가 곧 설득의 순서다.**

| # | 절이 답하는 질문 | 누가 | 왜 그 자리인가 |
|---|---|---|---|
| 1 | 무슨 일이 일어나고 있나 | 자동 | 다시 조회하면 같은 값이 나온다 |
| 2 | 어디서 벌어지나 | 자동 | *왜* 는 못 대고 *어디* 까지만 댄다 |
| 3 | 얼마짜리 문제인가 | 자동 | 제안보다 **앞**에 — 뒤에 두면 왜 해야 하는지 안 풀린 채로 읽는다 |
| 4 | 그래서 뭘 하자는 건가 | 자동 | 이미 적어 둔 것을 **옮기는** 일이다 |
| 5 | 틀리면 어떻게 되나 | **사람** | 요청보다 **앞**에 — 뒤에 두면 결정한 다음에 위험을 듣는다 |
| 6 | 나한테 뭘 해달라는 건가 | **사람** | 마지막 줄이라야 그 말이 남는다 |

| 그 밖에 정한 것 | 왜 |
|---|---|
| **인쇄되는 말은 한 곳에서만 바꾼다** | 여기저기 박으면 고칠 때 한 곳을 반드시 빠뜨리고, **빠뜨린 자리는 나가서야 보인다** |
| 고른 말 — 중단 검토 / 재측정 필요 / 추진 / 확인 필요 / 벌어짐 / 평소 변동폭 | 안에서 쓰는 분류는 그대로 두고 **인쇄되는 값만** 바꾼다. 안을 바꾸면 재료를 전부 고쳐야 한다 |
| **요청 문장은 사람이 쓰고 재료만 자동으로 붙인다** | 규모 · 선택지 셋 · 미뤘을 때 쌓이는 양. **무엇을 해달라는 말은 짓지 않는다** — 지으면 책임의 주체가 사라진다 |
| **결정 동사가 없으면 경고만 하고 저장은 막지 않는다** | 사람이 쓴 문장 때문에 저장이 막히면 **사람이 문장을 안 쓴다** |
| **확인 주체를 지어내지 않는다** | 모르면 묻는다. 두루뭉술한 말로 메우면 **아무도 자기 일인 줄 모른다** |
| **내보내기 앞에서 여섯을 센다. 고치지는 않는다** | 뒤에 두면 받아 간 뒤에 보게 되고, 그때는 이미 나간 뒤다 |

### 오늘 걸린 곳

| 걸린 것 | 무엇이 문제였나 |
|---|---|
| **검사가 주석을 위반으로 잡았다** | *"이것을 쓰지 않는다"* 라고 적은 주석이 그 말을 찾는 검사에 걸렸다. **규칙을 설명하는 글이 규칙을 어긴 꼴.** 주석을 지우면 왜 안 되는지가 사라지므로 **검사를 고쳤다** |
| **고친 검사가 아무것도 못 잡게 됐다** | 걸러야 할 것만 걸러야 하는데 볼 것까지 걸렀다. **자가검증 한 줄이 즉시 잡았다** — 없었으면 통과로 보였을 것이다 |
| **그림이 무엇을 그리는지 엉뚱한 데서 가져왔다** | 이름이 「구간」인 칸에 구간이 아닌 것이 들어 있었다. **그림을 그린 그 자료에서 뽑아야** 검사가 실제로 검사가 된다 |
| **볼 것이 없으면 검사가 조용히 통과했다** | 못 찾은 것은 **통과가 아니라 확인 못 함**이다. 통과로 세면 검사가 눈이 먼 채로 합격을 낸다 |
| **자동 문장이 사람의 편집칸에 들어갔다** | 한 절에 자동과 사람이 섞였는데 **첫 줄을 사람 것으로 가정**했다. 자동이 앞에 붙는 순간 어긋났다 |

### 이번에 쓴 방법

- **어디까지가 사람 말인지 절이 직접 들고 있게 한다.** 나중에 글자를 보고 가르면
  사람이 자동과 같은 말로 시작하는 문장을 쓰는 순간 어긋난다.
- **정본을 만드는 것과 정본을 쓰게 하는 것은 다르다.** 표만 만들고 코드를 안 고치면
  **옛말이 그대로 나간다.** 표를 만든 뒤 옛말이 몇 곳 남았는지 세어 본다.
- **검사기를 만들면 켜고 꺼 본다.** 심으면 잡히는가, 안 심으면 안 잡히는가 —
  **둘 다 봐야** 검사가 도는지 안다.
- **모르는 것은 물어보고 받은 답을 재료에 적는다.** 코드가 그 재료를 읽게 하면
  다음부터는 묻지 않아도 되고, **안 적으면 다음 사람이 또 짓는다.**

---

## 2026-09-11 (Day4 진도) — 남이 읽고 결정하는 쪽에서

어제까지는 **내가 내 문서를 검사**했다. 오늘 처음 **남이 읽고 판정**했다.
검사를 통과한 문서가 **결정을 못 받아냈다.**

### 배운 것

- **읽히는 문서와 결정이 나오는 문서는 다르다.** 검사 항목을 다 통과해도
  결재자가 *"이걸로는 판단이 안 된다"* 고 하면 그 문서는 제 일을 못 한 것이다.
- **지적을 전부 고치면 판단을 안 한 것이다.** 셋으로 가른다 —
  없다(고친다) / **있는데 못 찾았다(자리를 옮긴다)** / 내 사정을 모른다(안 고치고 이유를 적는다).
  가운데가 핵심이다. **내용이 아니라 배치 문제**라 새로 쓰면 같은 말이 두 곳에 생긴다.
- ★ **벤치마크는 문서가 잘 쓰였는지를 잰다. 제안이 옳은지는 재지 못한다.**
  점수가 올라도 근거가 틀렸으면 틀린 문서다. 근거의 진위는 **다른 날 다른 방법으로** 확인한다.
- ★ **한계와 부재는 다르다.** *"정확하지 않다"* 를 *"없다"* 로 읽으면 낼 수 있는 것을
  안 내게 되고, **그 자리에서 결재가 막힌다.** 없다고 적기 전에 실제로 조회한다.
- **집계 단위를 바꿀 때는 실제 시스템에 그 식별자가 있는지부터 본다.**
  연습용으로 만든 자료에는 내가 붙인 키가 있어 문제가 안 보인다 —
  **실제 자료로 옮기는 첫 줄에서 막힌다.**
- **표본이 작은 것을 여럿 동시에 보면 우연이 섞인다.** 하나씩 보면 낮아 보여도,
  천 개를 한꺼번에 보면 그중 몇 개가 낮게 나오는 것이 오히려 정상이다.

### 오늘 내가 정한 것

| 정한 것 | 왜 |
|---|---|
| **판정은 보류로 받았고, 그 사유를 고칠 것으로 삼는다** | 결재자가 멈춘 자리가 곧 문서의 빈 곳이다 |
| **안 고친 것 하나 — 연습용 자료라는 지적** | 이유: **판정이 아니라 방법을 보이는 문서**라서다. 실자료가 들어오면 같은 절차로 다시 잰다 |
| **없다고 단정했던 값을 조회해서 낸다** | 한 달 동안 *"이 영역에는 없다"* 고 절을 안 만들었는데 자료에 처음부터 있었다 |
| **나가는 문서와 소스는 감추는 것이 다르다** | 근거의 출처는 소스에 남겨야 그 숫자를 믿을 수 있다. **나가는 쪽에서만** 가린다 |
| **집계 단위를 실제 시스템에 있는 식별자로 옮긴다** | 바꿔도 전체 값은 0.02%p 밖에 안 움직였다. 바뀌는 것은 **무엇을 표적으로 삼을 수 있는가** 다 |
| **모르는 것에 기한을 붙인다** | *"확인이 필요하다"* 만 적으면 읽는 사람이 **언제까지인지를 되묻는다** |

### 오늘 걸린 곳

| 걸린 것 | 무엇이 문제였나 |
|---|---|
| **승인할 대상의 이름이 문서에 없었다** | 분류별 **개수만** 적고 제목은 한 묶음만 실었다. 내용은 재료에 있었는데 **안 내보냈다** |
| **같은 숫자를 세 번 반복했는데 결재가 안 났다** | 더 반복할 것이 아니라 **단위를 바꿔야** 했다. 읽는 사람이 쓰는 단위로 말하지 않으면 몇 번을 말해도 안 통한다 |
| **곱셈이 조용히 감겼다** | 작은 정수형끼리 곱해 범위를 넘었는데 **오류가 안 났다.** 값만 틀렸고 자릿수가 작다는 것 말고는 아무 신호도 없었다 |
| **쪼갠 축이 다른 축의 대리변수였다** | 한 쪽으로 나눈 결과가 실은 다른 쪽 차이였다. **쪼개기 전에 그 축이 무엇의 대리인지** 본다 |
| ★ **"더 잘 보인다"고 한 것이 우연이었다** | 표적을 새 단위로 옮기면 개선이 커 보였는데, 다중 비교로 보정하니 **우연 기대치보다 적었다.** 내가 오늘 점검한 함정에 내가 걸렸다 |

### 7주차 함정 점검 (실습 F)

| 함정 | 오늘 문서에서 |
|---|---|
| ① 집계 단위 오류 | ⚠️ **걸렸다 → 고쳤다.** 단위가 문서에 한 번도 안 적혀 있었고, 서로 다른 모집단의 숫자가 설명 없이 섞여 있었다 |
| ② 지켜야 할 선 없는 성공 | ⚠️ **걸렸다 — 남겼다.** 주지표를 끌어올리자는 제안에 **부작용을 재는 지표가 없다.** 철회 기준은 있는데 그것은 다른 것이다 |
| ③ 실험 설계 | ○ 절반. **왜** 다시 재는지는 적혔고 **얼마나**(표본·기간)가 안 적혔다 |
| ④ 상관을 인과로 | ○ 문장은 안전(인과 단정 표현 0). ⚠️ **값이 위험** — 오늘 넣은 분해가 대리변수였다 |

### 이번에 쓴 방법

- **"있는데 못 찾았다"는 실제로 대조한다.** 문서 전체와 재료를 훑어 **어느 절 몇 번째
  문장에 있는지**까지 짚는다. *"고쳤다"* 는 말로 세지 않는다.
- **검사기가 스스로 판정하게 만든다.** 쓰는 쪽이 통계를 몰라도 되게 판정을 **값으로
  싣는다** — *"우연과 구분 안 됨"* 이 표에 찍혀 나오면 아무도 그것을 표적으로 안 삼는다.
- **검사기를 켜고 꺼서 둘 다 본다.** 심으면 잡히는가, 안 심으면 안 잡히는가.
  하나만 보면 **눈먼 검사가 만점을 받는다.**
- **우려는 말하되 데이터로 확인한다.** 오늘 두 번 우려했고 두 번 다 데이터가
  내 말을 뒤집었다. **말로 막았으면 둘 다 못 했을 것이다.**

## 2026-09-12 (Day5 오후) — 도번 단위로 전부 다시 쓴다

### 오늘 내가 정한 것

| 정한 것 | 왜 |
|---|---|
| **세는 단위는 제품도번. 퍼널은 "멈춘 발주가 있는 도번 수"로 읽는다** | 도번 고유값으로 도달을 세면 11단계 전부 1,200개라 정보가 없다(실측). 도번 단위의 사실은 *어느 구간에서 멈춘 발주가 있는 도번이 몇 개인가*다 — 검사→출하 754/1,200 |
| **축별 준수율은 도번 평균, 크기는 발주 건** | 도번 하나를 하나로 센다(발주 많은 도번이 칸을 끌지 않게). 도번에 "건수"는 없으니 격차 × 그쪽 발주 수로 환산 — V6 3.08%p × 9,523 = 293건 |
| **발주 단위 함수는 지우지 않는다 — 재료다** | `part_facts` 가 `order_facts` 를 재료로 쓰듯. 두 값의 차이(0.07%p 이내)를 표에 같이 둔다 — 바뀐 것은 단위지 결론이 아니라는 증거 |
| **전면 지시는 적용 대상 전수 목록부터** | "모든 기준은 도번으로"를 집계 함수만 바꾸고 절반을 내 해석으로 남겼다가 5시간 뒤 정정 — 안 되는 곳은 숫자로 보이고 묻는다 |
