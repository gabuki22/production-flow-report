# 공개 저장소에 내보내지 않는 것.
#
# ★ 이 목록은 **검사가 훑지 않는 목록과 같다** (tests/test_guards.py 11절).
#   안 훑는 것을 내보내면 무엇이 새는지 아무도 안 본 채로 나간다.
#   그래서 목록을 두 곳에 두지 않고 여기 하나만 둔다.
#
# 상대경로의 일부만 적으면 된다(부분 일치).

# 강사가 PDF 로 따로 배포한 참고 문서 4종 — 우리가 다시 낼 것이 아니다
_참고자료

# 강사가 배포한 제안서 템플릿·예시 — _참고자료 와 같은 이유로 우리가 다시 낼 것이 아니다
#   ⚠️ 앱은 이 폴더를 읽는다(proposal.template_css). 없으면 임시 형식으로 물러나고
#      그 사실을 문서 맨 위에 적는다 — 조용히 다른 모양으로 나가지 않는다
수업자료

# 작업 흔적(백업·변경이력). 앱이 읽지 않는다
_작업보조

# 내부 검토 문서와 미리보기 산출물
#   drafts/human.json 은 리포트 2·6·8장 본문이라 **내보낸다**
drafts/검토
drafts/미리보기

__pycache__
.streamlit/secrets.toml
